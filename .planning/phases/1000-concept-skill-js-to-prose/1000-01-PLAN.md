---
phase: 1000-concept-skill-js-to-prose
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/concept/SKILL.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "All ```javascript fences in Phase 1 (lines 271–1127) are replaced with prose bullet lists or compact tables"
    - "All ```javascript fences in Phase 2 (lines 1128–1565) are replaced with prose bullet lists or compact tables"
    - "All ```javascript fences in Phase 2.5 (lines 1566–2035) are replaced with prose bullet lists or compact tables"
    - "Scoring weights (50/30/20), field names, and algorithm steps from Phase 1-2.5 JS are preserved in prose form"
    - "No new content or behavior has been introduced — format change only"
  artifacts:
    - path: "skills/concept/SKILL.md"
      provides: "Phase 1, 2, 2.5 sections rewritten to prose"
      contains: "## PHASE 1: REQUIREMENTS GATHERING"
  key_links:
    - from: "skills/concept/SKILL.md Phase 1"
      to: "prose instructions"
      via: "bullet lists and tables replacing JS functions"
      pattern: "no ```javascript fence between lines 271-2035"
---

<objective>
Rewrite Phase 1 (REQUIREMENTS GATHERING), Phase 2 (ARCHITECTURE DRAFTING), and Phase 2.5 (REQUIREMENTS-TO-COMPONENT MAPPING) sections of `skills/concept/SKILL.md` — replacing all JavaScript pseudo-code fences with concise prose instructions.

Purpose: These three phases contain 833 JS lines (219 + 293 + 321). Converting them to prose eliminates dead weight Claude reads but never executes, while preserving every field name, threshold, weight, and algorithm step.

Output: Lines 271–2035 of `skills/concept/SKILL.md` rewritten. No JS fences remain in those sections.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/1000-concept-skill-js-to-prose/1000-CONTEXT.md

Key constraints from CONTEXT.md:
- D-01: Preserve ALL field names, scoring weights, diversity thresholds, iteration limits, algorithm steps, data structures
- D-02: Format change only — behavioral fidelity is the success criterion
- D-03: Keep 9-phase structure and headers intact
- D-04: Consolidate sub-sections where JS removal leaves a thin header (no new content, no phase merges)
- D-06: One task per SKILL.md phase — each task is atomic and independently reviewable
- D-08: Each task operates on the exact line range of its phase section

Prose rules (from CONTEXT.md specifics):
- Replacement for a JS function: 3–10 bullet points or a compact table (not paragraph essays)
- Style: imperative/instructional — "Do X when Y" not "The function does X when Y"
- Every ```javascript fence must be gone — any remaining JS fence is a bug
- Non-JS content (markdown templates, YAML examples, prose overviews) must be preserved as-is
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite Phase 1 — REQUIREMENTS GATHERING (lines 271–1127)</name>
  <read_first>
    - skills/concept/SKILL.md (lines 271–1127 — the exact section being rewritten; read in full before touching anything)
  </read_first>
  <files>skills/concept/SKILL.md</files>
  <action>
Rewrite the Phase 1 section (lines 271–1127) of skills/concept/SKILL.md. This section contains approximately 219 JS lines across multiple functions. Before writing, read the entire section to catalog every JS block and the logic it encodes.

Replacement rules:
1. For each ```javascript ... ``` block, extract the logic and convert to imperative prose. Use 3–10 bullet points or a compact table. Preserve exact field names (e.g., `constraints`, `interface_types`, `power_budget`), scoring weights, thresholds, and algorithm steps verbatim.
2. Style: "Do X when Y" not "The function does X when Y."
3. If removing a JS block leaves a header with trivially thin body (the header only existed to introduce the function), inline the header text into the prose bullet list and remove the now-empty header (per D-04). Never merge or split phase boundaries.
4. All non-JS content (existing prose paragraphs, YAML examples, markdown templates) must be preserved unchanged.
5. After rewriting, verify: `grep -n '```javascript' skills/concept/SKILL.md` must show NO matches in lines 271–1127.

Example conversion pattern:
BEFORE:
```javascript
function scoreRequirement(req) {
  if (req.priority === 'critical') return 50;
  if (req.priority === 'important') return 30;
  return 20;
}
```
AFTER:
- Score each requirement by priority: critical = 50, important = 30, nice-to-have = 20.

Operate on the file with targeted edits — replace only the JS fences and their content within lines 271–1127. Do not touch lines outside this range.
  </action>
  <verify>
    <automated>grep -n '```javascript' /home/william/repo/LibreSpin/LibreSpin/skills/concept/SKILL.md | awk -F: '$1 >= 271 && $1 <= 1127'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n '```javascript' skills/concept/SKILL.md | awk -F: '$1 >= 271 && $1 <= 1127'` returns empty output
    - `grep -c '## PHASE 1: REQUIREMENTS GATHERING' skills/concept/SKILL.md` returns 1 (header preserved)
    - The scoring weights 50, 30, 20 appear in prose form in the Phase 1 section
    - No line in range 271–1127 contains the text "function " followed by "()" (JS function declarations gone)
  </acceptance_criteria>
  <done>Phase 1 section (lines 271–1127) contains zero JS fences. All logic preserved as imperative prose bullet lists or tables. Phase header intact.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite Phase 2 — ARCHITECTURE DRAFTING (lines 1128–1565)</name>
  <read_first>
    - skills/concept/SKILL.md (lines 1128–1565 — the exact section being rewritten; read in full before touching anything)
  </read_first>
  <files>skills/concept/SKILL.md</files>
  <action>
Rewrite the Phase 2 section (lines 1128–1565) of skills/concept/SKILL.md. This section contains approximately 293 JS lines. Before writing, read the entire section to catalog every JS block and the logic it encodes.

Apply the same rules as Task 1:
1. Convert each ```javascript ... ``` block to 3–10 imperative prose bullet points or a compact table.
2. Preserve exact field names, data structure keys, scoring weights, thresholds, and algorithm steps verbatim.
3. Consolidate thin headers left behind by JS removal (D-04) — no new content, no phase merges.
4. Non-JS content preserved unchanged.
5. After rewriting: `grep -n '```javascript' skills/concept/SKILL.md` must show NO matches in lines 1128–1565.

Operate on lines 1128–1565 only. Do not touch Phase 1 (already rewritten) or Phase 2.5 (next task).
  </action>
  <verify>
    <automated>grep -n '```javascript' /home/william/repo/LibreSpin/LibreSpin/skills/concept/SKILL.md | awk -F: '$1 >= 1128 && $1 <= 1565'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n '```javascript' skills/concept/SKILL.md | awk -F: '$1 >= 1128 && $1 <= 1565'` returns empty output
    - `grep -c '## PHASE 2: ARCHITECTURE DRAFTING' skills/concept/SKILL.md` returns 1 (header preserved)
    - No line in range 1128–1565 contains the text "function " followed by a word and "()" pattern
    - Section still contains prose describing architecture drafting concepts (not a blank section)
  </acceptance_criteria>
  <done>Phase 2 section (lines 1128–1565) contains zero JS fences. All architecture drafting logic preserved as prose. Phase header intact.</done>
</task>

<task type="auto">
  <name>Task 3: Rewrite Phase 2.5 — REQUIREMENTS-TO-COMPONENT MAPPING (lines 1566–2035)</name>
  <read_first>
    - skills/concept/SKILL.md (lines 1566–2035 — the exact section being rewritten; read in full before touching anything)
  </read_first>
  <files>skills/concept/SKILL.md</files>
  <action>
Rewrite the Phase 2.5 section (lines 1566–2035) of skills/concept/SKILL.md. This section contains approximately 321 JS lines. Before writing, read the entire section to catalog every JS block and the logic it encodes.

Apply the same rules as Tasks 1 and 2:
1. Convert each ```javascript ... ``` block to 3–10 imperative prose bullet points or a compact table.
2. Preserve exact field names, component mapping keys, scoring logic, thresholds, and algorithm steps verbatim.
3. Consolidate thin headers left behind by JS removal (D-04) — no new content, no phase merges.
4. Non-JS content preserved unchanged.
5. After rewriting: `grep -n '```javascript' skills/concept/SKILL.md` must show NO matches in lines 1566–2035.

Operate on lines 1566–2035 only. Do not touch Phase 2 (already rewritten) or Phase 3 (separate plan).
  </action>
  <verify>
    <automated>grep -n '```javascript' /home/william/repo/LibreSpin/LibreSpin/skills/concept/SKILL.md | awk -F: '$1 >= 1566 && $1 <= 2035'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n '```javascript' skills/concept/SKILL.md | awk -F: '$1 >= 1566 && $1 <= 2035'` returns empty output
    - `grep -c '## PHASE 2.5: REQUIREMENTS-TO-COMPONENT MAPPING' skills/concept/SKILL.md` returns 1 (header preserved)
    - No line in range 1566–2035 contains "function " followed by a word and "()"
    - Section still contains mapping instructions relating requirements to components
  </acceptance_criteria>
  <done>Phase 2.5 section (lines 1566–2035) contains zero JS fences. All component mapping logic preserved as prose. Phase header intact.</done>
</task>

</tasks>

<verification>
After all three tasks complete:
- `grep -c '```javascript' skills/concept/SKILL.md` returns a count lower than 125 (original total) — specifically, lines 271–2035 contribute 0 JS fences
- All three phase headers still present: `grep -c '^## PHASE 1:\|^## PHASE 2:\|^## PHASE 2\.5:' skills/concept/SKILL.md` returns 3
- File is smaller than at plan start (lines reduced, not grown)
</verification>

<success_criteria>
Lines 271–2035 of skills/concept/SKILL.md contain zero ```javascript fences. All scoring weights, field names, thresholds, and algorithm logic preserved as imperative prose bullet lists or compact tables. Phase headers (PHASE 1, PHASE 2, PHASE 2.5) intact.
</success_criteria>

<output>
After completion, create `.planning/phases/1000-concept-skill-js-to-prose/1000-01-SUMMARY.md`
</output>
