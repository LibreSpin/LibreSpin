---
phase: 1000-concept-skill-js-to-prose
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/concept/SKILL.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "All ```javascript fences in Phase 3 (lines 2036–3068) are replaced with prose bullet lists or compact tables"
    - "All ```javascript fences in Phase 4 (lines 3069–4839) are replaced with prose bullet lists or compact tables"
    - "Validation gate logic, pass/fail thresholds, component research scoring from Phases 3-4 JS are preserved in prose form"
    - "No new content or behavior has been introduced — format change only"
  artifacts:
    - path: "skills/concept/SKILL.md"
      provides: "Phase 3 and Phase 4 sections rewritten to prose"
      contains: "## PHASE 3: VALIDATION GATE"
  key_links:
    - from: "skills/concept/SKILL.md Phase 3-4"
      to: "prose instructions"
      via: "bullet lists and tables replacing JS functions"
      pattern: "no ```javascript fence between lines 2036-4839"
---

<objective>
Rewrite Phase 3 (VALIDATION GATE) and Phase 4 (COMPONENT RESEARCH) sections of `skills/concept/SKILL.md` — replacing all JavaScript pseudo-code fences with concise prose instructions.

Purpose: These two phases are the heaviest JS sections: 492 JS lines in Phase 3 and 1039 JS lines in Phase 4 (1531 combined). Phase 4 alone accounts for nearly a third of all JS in the file. Converting to prose is the highest-leverage rewrite in this phase.

Output: Lines 2036–4839 of `skills/concept/SKILL.md` rewritten. No JS fences remain in those sections.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/1000-concept-skill-js-to-prose/1000-CONTEXT.md

Key constraints from CONTEXT.md:
- D-01: Preserve ALL field names, scoring weights, diversity thresholds, iteration limits, algorithm steps, data structures
- D-02: Format change only — behavioral fidelity is the success criterion
- D-03: Keep 9-phase structure and headers intact
- D-04: Consolidate sub-sections where JS removal leaves a thin header (no new content, no phase merges)
- D-06: One task per SKILL.md phase — each task is atomic and independently reviewable
- D-08: Each task operates on the exact line range of its phase section

Prose rules:
- Replacement for a JS function: 3–10 bullet points or a compact table (not paragraph essays)
- Style: imperative/instructional — "Do X when Y" not "The function does X when Y"
- Every ```javascript fence must be gone — any remaining JS fence is a bug
- Non-JS content (markdown templates, YAML examples, prose overviews) must be preserved as-is

NOTE: Plan 02 runs in Wave 1 parallel with Plan 01 and Plan 03. Plans 01-03 each own non-overlapping line ranges of skills/concept/SKILL.md. This plan owns lines 2036–4839.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite Phase 3 — VALIDATION GATE (lines 2036–3068)</name>
  <read_first>
    - skills/concept/SKILL.md (lines 2036–3068 — the exact section being rewritten; read in full before touching anything)
  </read_first>
  <files>skills/concept/SKILL.md</files>
  <action>
Rewrite the Phase 3 section (lines 2036–3068) of skills/concept/SKILL.md. This section contains approximately 492 JS lines. Before writing, read the entire section to catalog every JS block and the logic it encodes.

Replacement rules:
1. For each ```javascript ... ``` block, extract the logic and convert to imperative prose. Use 3–10 bullet points or a compact table. Preserve exact field names, validation pass/fail thresholds, error codes, score cutoffs, and decision logic verbatim.
2. Style: "Do X when Y" not "The function does X when Y."
3. If removing a JS block leaves a header with trivially thin body, inline the header text into the prose and remove the now-empty header (per D-04). Never merge or split phase boundaries.
4. All non-JS content (existing prose paragraphs, YAML examples, markdown templates, output file specs) must be preserved unchanged.
5. After rewriting, verify: no ```javascript fences remain in lines 2036–3068.

Operate on lines 2036–3068 only. Do not touch lines outside this range.
  </action>
  <verify>
    <automated>grep -n '```javascript' /home/william/repo/LibreSpin/LibreSpin/skills/concept/SKILL.md | awk -F: '$1 >= 2036 && $1 <= 3068'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n '```javascript' skills/concept/SKILL.md | awk -F: '$1 >= 2036 && $1 <= 3068'` returns empty output
    - `grep -c '## PHASE 3: VALIDATION GATE' skills/concept/SKILL.md` returns 1 (header preserved)
    - No line in range 2036–3068 contains the text "function " followed by a word character and "()"
    - Section still contains validation gate instructions (pass/fail logic preserved in prose)
  </acceptance_criteria>
  <done>Phase 3 section (lines 2036–3068) contains zero JS fences. All validation gate logic preserved as imperative prose. Phase header intact.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite Phase 4 — COMPONENT RESEARCH (lines 3069–4839)</name>
  <read_first>
    - skills/concept/SKILL.md (lines 3069–4839 — the exact section being rewritten; read in full before touching anything)
  </read_first>
  <files>skills/concept/SKILL.md</files>
  <action>
Rewrite the Phase 4 section (lines 3069–4839) of skills/concept/SKILL.md. This is the largest JS section with approximately 1039 JS lines. This is the highest-value rewrite task. Before writing, read the entire section to catalog every JS block and the logic it encodes.

Phase 4 likely contains BOM construction logic, component scoring/selection algorithms, distributor lookup sequencing, MPN matching, and fallback rules. All of these must be preserved as prose. Pay particular attention to:
- Component selection scoring weights and criteria
- Diversity/distribution thresholds (how many concepts must differ)
- Iteration limits (e.g., max retries before falling back)
- Data structure field names used in BOM entries
- Any table structures describing component categories, filters, or search parameters

Apply the same conversion rules:
1. Convert each ```javascript ... ``` block to 3–10 imperative prose bullet points or a compact table.
2. Preserve exact field names, weights, thresholds, and algorithm steps verbatim.
3. Consolidate thin headers left behind by JS removal (D-04) — no new content, no phase merges.
4. Non-JS content (output templates, YAML examples, markdown structure) preserved unchanged.
5. After rewriting: no ```javascript fences remain in lines 3069–4839.

Operate on lines 3069–4839 only. Do not touch Phase 3 (already rewritten in Task 1) or Phase 5 (separate plan).
  </action>
  <verify>
    <automated>grep -n '```javascript' /home/william/repo/LibreSpin/LibreSpin/skills/concept/SKILL.md | awk -F: '$1 >= 3069 && $1 <= 4839'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n '```javascript' skills/concept/SKILL.md | awk -F: '$1 >= 3069 && $1 <= 4839'` returns empty output
    - `grep -c '## PHASE 4: COMPONENT RESEARCH' skills/concept/SKILL.md` returns 1 (header preserved)
    - No line in range 3069–4839 contains the text "function " followed by a word character and "()"
    - Section still contains component research/BOM instructions (not a blank section)
    - BOM field names from the original JS are present in the prose (verify by checking at least one original field name survived)
  </acceptance_criteria>
  <done>Phase 4 section (lines 3069–4839) contains zero JS fences. All component research and BOM selection logic preserved as imperative prose. Phase header intact.</done>
</task>

</tasks>

<verification>
After both tasks complete:
- `grep -c '```javascript' skills/concept/SKILL.md` is lower than it was before this plan ran (lines 2036–4839 contribute 0)
- Both phase headers present: `grep -c '^## PHASE 3:\|^## PHASE 4:' skills/concept/SKILL.md` returns 2
- File contains prose instructions for validation gate and component research (not blank sections)
</verification>

<success_criteria>
Lines 2036–4839 of skills/concept/SKILL.md contain zero ```javascript fences. All validation thresholds, component scoring weights, BOM field names, and algorithm logic preserved as imperative prose. Phase headers (PHASE 3, PHASE 4) intact.
</success_criteria>

<output>
After completion, create `.planning/phases/1000-concept-skill-js-to-prose/1000-02-SUMMARY.md`
</output>
