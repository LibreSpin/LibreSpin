---
phase: 1000-concept-skill-js-to-prose
plan: 03
type: execute
wave: 3
depends_on:
  - "1000-02"
files_modified:
  - skills/concept/SKILL.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "All ```javascript fences in Phase 5 (lines 4840–5471) are replaced with prose bullet lists or compact tables"
    - "All ```javascript fences in Phase 6 (lines 5472–6667) are replaced with prose bullet lists or compact tables"
    - "All ```javascript fences in Phase 7 (lines 6668–7388) are replaced with prose bullet lists or compact tables"
    - "Concept generation scoring, self-critique iteration limits, refinement logic, and final output format rules from Phases 5-7 JS are preserved in prose form"
    - "No new content or behavior has been introduced — format change only"
  artifacts:
    - path: "skills/concept/SKILL.md"
      provides: "Phase 5, 6, 7 sections rewritten to prose"
      contains: "## PHASE 5: CONCEPT GENERATION"
  key_links:
    - from: "skills/concept/SKILL.md Phase 5-7"
      to: "prose instructions"
      via: "bullet lists and tables replacing JS functions"
      pattern: "no ```javascript fence between lines 4840-7388"
---

<objective>
Rewrite Phase 5 (CONCEPT GENERATION), Phase 6 (SELF-CRITIQUE & REFINEMENT), and Phase 7 (FINAL OUTPUT) sections of `skills/concept/SKILL.md` — replacing all JavaScript pseudo-code fences with concise prose instructions.

Purpose: These three phases contain 1880 JS lines (453 + 787 + 640). Phase 6 and Phase 7 together are the second-heaviest JS sections after Phase 4. Converting them to prose eliminates dead weight while preserving all diversity thresholds, iteration limits, scoring logic, and output format rules.

Output: Lines 4840–7388 of `skills/concept/SKILL.md` rewritten. No JS fences remain in those sections.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/1000-concept-skill-js-to-prose/1000-CONTEXT.md

Key constraints from CONTEXT.md:
- D-01: Preserve ALL field names, scoring weights, diversity thresholds, iteration limits, algorithm steps, data structures
- D-02: Format change only — behavioral fidelity is the success criterion
- D-03: Keep 9-phase structure and headers intact
- D-04: Consolidate sub-sections where JS removal leaves a thin header (no new content, no phase merges)
- D-06: One task per SKILL.md phase — each task is atomic and independently reviewable
- D-08: Each task operates on the exact line range of its phase section

Prose rules:
- Replacement for a JS function: 3–10 bullet points or a compact table (not paragraph essays)
- Style: imperative/instructional — "Do X when Y" not "The function does X when Y"
- Every ```javascript fence must be gone — any remaining JS fence is a bug
- Non-JS content (markdown templates, YAML examples, prose overviews) must be preserved as-is

NOTE: Plan 03 runs in Wave 3, after Plan 02. Plans execute sequentially (01→02→03) to prevent concurrent writes to skills/concept/SKILL.md.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite Phase 5 — CONCEPT GENERATION (lines 4840–5471)</name>
  <read_first>
    - skills/concept/SKILL.md (lines 4840–5471 — the exact section being rewritten; read in full before touching anything)
  </read_first>
  <files>skills/concept/SKILL.md</files>
  <action>
Rewrite the Phase 5 section (lines 4840–5471) of skills/concept/SKILL.md. This section contains approximately 453 JS lines. Before writing, read the entire section to catalog every JS block and the logic it encodes.

Phase 5 generates hardware concept designs. Pay particular attention to:
- The number of concepts to generate (target count)
- Diversity thresholds (how much concepts must differ from each other)
- Scoring weights applied when ranking concepts
- Data structure fields for each concept (name, description, BOM, block diagram refs)
- Any loop/iteration logic controlling how many attempts before accepting

Replacement rules:
1. Convert each ```javascript ... ``` block to 3–10 imperative prose bullet points or a compact table.
2. Preserve exact field names, counts, weights, and thresholds verbatim.
3. Consolidate thin headers per D-04 — no new content, no phase merges.
4. Non-JS content preserved unchanged.
5. After rewriting: no ```javascript fences remain in lines 4840–5471.

Operate on lines 4840–5471 only.
  </action>
  <verify>
    <automated>grep -n '```javascript' /home/william/repo/LibreSpin/LibreSpin/skills/concept/SKILL.md | awk -F: '$1 >= 4840 && $1 <= 5471'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n '```javascript' skills/concept/SKILL.md | awk -F: '$1 >= 4840 && $1 <= 5471'` returns empty output
    - `grep -c '## PHASE 5: CONCEPT GENERATION' skills/concept/SKILL.md` returns 1 (header preserved)
    - No line in range 4840–5471 contains "function " followed by a word character and "()"
    - Section still contains concept generation instructions (not a blank section)
  </acceptance_criteria>
  <done>Phase 5 section (lines 4840–5471) contains zero JS fences. All concept generation logic preserved as imperative prose. Phase header intact.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite Phase 6 — SELF-CRITIQUE & REFINEMENT (lines 5472–6667)</name>
  <read_first>
    - skills/concept/SKILL.md (lines 5472–6667 — the exact section being rewritten; read in full before touching anything)
  </read_first>
  <files>skills/concept/SKILL.md</files>
  <action>
Rewrite the Phase 6 section (lines 5472–6667) of skills/concept/SKILL.md. This section contains approximately 787 JS lines — the second-heaviest JS section in the file. Before writing, read the entire section to catalog every JS block.

Phase 6 implements self-critique and refinement loops. Pay particular attention to:
- Max iteration count for refinement loops
- Criteria used to evaluate whether a concept passes self-critique
- Thresholds for when to stop iterating (convergence criteria)
- Scoring adjustments applied during refinement
- Data fields modified by the refinement process
- Exit conditions and fallback behavior when max iterations reached

Replacement rules:
1. Convert each ```javascript ... ``` block to 3–10 imperative prose bullet points or a compact table.
2. Preserve exact iteration limits, scoring rules, threshold values, and field names verbatim.
3. Consolidate thin headers per D-04 — no new content, no phase merges.
4. Non-JS content preserved unchanged.
5. After rewriting: no ```javascript fences remain in lines 5472–6667.

Operate on lines 5472–6667 only. Do not touch Phase 5 (already rewritten) or Phase 7 (next task).
  </action>
  <verify>
    <automated>grep -n '```javascript' /home/william/repo/LibreSpin/LibreSpin/skills/concept/SKILL.md | awk -F: '$1 >= 5472 && $1 <= 6667'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n '```javascript' skills/concept/SKILL.md | awk -F: '$1 >= 5472 && $1 <= 6667'` returns empty output
    - `grep -c '## PHASE 6: SELF-CRITIQUE' skills/concept/SKILL.md` returns 1 (header preserved)
    - No line in range 5472–6667 contains "function " followed by a word character and "()"
    - Section still contains self-critique and refinement instructions (not a blank section)
    - Iteration limits and convergence criteria visible in prose form
  </acceptance_criteria>
  <done>Phase 6 section (lines 5472–6667) contains zero JS fences. All self-critique and refinement loop logic preserved as imperative prose. Phase header intact.</done>
</task>

<task type="auto">
  <name>Task 3: Rewrite Phase 7 — FINAL OUTPUT (lines 6668–7388)</name>
  <read_first>
    - skills/concept/SKILL.md (lines 6668–7388 — the exact section being rewritten; read in full before touching anything)
  </read_first>
  <files>skills/concept/SKILL.md</files>
  <action>
Rewrite the Phase 7 section (lines 6668–7388) of skills/concept/SKILL.md. This section contains approximately 640 JS lines. Before writing, read the entire section to catalog every JS block.

Phase 7 produces the final output artifacts. Pay particular attention to:
- Output file paths and naming conventions
- Required fields in each output file (block diagram, BOM, concept summary)
- Formatting rules for the output markdown files
- Scoring display format for final concepts
- Any conditions controlling which concepts appear in final output
- Pass/fail thresholds for output acceptance

Replacement rules:
1. Convert each ```javascript ... ``` block to 3–10 imperative prose bullet points or a compact table.
2. Preserve exact file path patterns, field names, format rules, and thresholds verbatim.
3. Consolidate thin headers per D-04 — no new content, no phase merges.
4. Non-JS content (output file templates, YAML examples) preserved unchanged.
5. After rewriting: no ```javascript fences remain in lines 6668–7388.

Operate on lines 6668–7388 only. Do not touch Phase 6 (already rewritten) or the PHASE DISPATCH section (lines 7389+, contains no JS — leave untouched).
  </action>
  <verify>
    <automated>grep -n '```javascript' /home/william/repo/LibreSpin/LibreSpin/skills/concept/SKILL.md | awk -F: '$1 >= 6668 && $1 <= 7388'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n '```javascript' skills/concept/SKILL.md | awk -F: '$1 >= 6668 && $1 <= 7388'` returns empty output
    - `grep -c '## PHASE 7: FINAL OUTPUT' skills/concept/SKILL.md` returns 1 (header preserved)
    - `grep -c '## PHASE DISPATCH' skills/concept/SKILL.md` returns 1 (DISPATCH section untouched)
    - No line in range 6668–7388 contains "function " followed by a word character and "()"
    - Section still contains final output instructions including output file specifications
  </acceptance_criteria>
  <done>Phase 7 section (lines 6668–7388) contains zero JS fences. All final output logic and file format rules preserved as imperative prose. Phase header intact. PHASE DISPATCH section untouched.</done>
</task>

</tasks>

<verification>
After all three tasks complete:
- `grep -c '```javascript' skills/concept/SKILL.md` is lower than it was before this plan ran (lines 4840–7388 contribute 0)
- All three phase headers present: `grep -c '^## PHASE 5:\|^## PHASE 6:\|^## PHASE 7:' skills/concept/SKILL.md` returns 3
- PHASE DISPATCH header still present: `grep -c '^## PHASE DISPATCH' skills/concept/SKILL.md` returns 1
- File contains prose instructions for concept generation, self-critique, and final output
</verification>

<success_criteria>
Lines 4840–7388 of skills/concept/SKILL.md contain zero ```javascript fences. All concept generation diversity thresholds, self-critique iteration limits, refinement scoring, and final output format rules preserved as imperative prose. Phase headers (PHASE 5, PHASE 6, PHASE 7) intact. PHASE DISPATCH untouched.
</success_criteria>

<output>
After completion, create `.planning/phases/1000-concept-skill-js-to-prose/1000-03-SUMMARY.md`
</output>
