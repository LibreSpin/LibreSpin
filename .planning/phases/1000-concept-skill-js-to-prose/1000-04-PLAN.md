---
phase: 1000-concept-skill-js-to-prose
plan: 04
type: execute
wave: 2
depends_on:
  - "1000-01"
  - "1000-02"
  - "1000-03"
files_modified: []
autonomous: false
requirements: []
must_haves:
  truths:
    - "Zero ```javascript fences remain anywhere in skills/concept/SKILL.md"
    - "All 9 phase headers are present and intact"
    - "File size is substantially reduced from 254KB (target ~35KB)"
    - "Running /librespin:concept on an IoT sensor node project produces 5 or more concepts with BOM, block diagram, and scored output"
  artifacts:
    - path: "skills/concept/SKILL.md"
      provides: "Fully rewritten prose-only skill file"
      contains: "## PHASE DISPATCH"
  key_links:
    - from: "skills/concept/SKILL.md"
      to: "concept workflow output"
      via: "/librespin:concept invocation"
      pattern: "5+ concepts with BOM entries and scored output"
---

<objective>
Verify the complete JS-to-prose rewrite across all plans (1000-01, 1000-02, 1000-03): confirm zero JS fences remain, all phase headers are intact, file size is substantially reduced, then run `/librespin:concept` on a synthetic IoT sensor node project to gate behavioral fidelity.

Purpose: D-09/D-10 validation gate — the rewrite is not done until the skill demonstrably still works. A regression here means one or more prose rewrites dropped required logic.

Output: Confirmed working prose-only SKILL.md. Human approval or regression report.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/1000-concept-skill-js-to-prose/1000-CONTEXT.md
@.planning/phases/1000-concept-skill-js-to-prose/1000-01-SUMMARY.md
@.planning/phases/1000-concept-skill-js-to-prose/1000-02-SUMMARY.md
@.planning/phases/1000-concept-skill-js-to-prose/1000-03-SUMMARY.md

Key constraints:
- D-09: Run /librespin:concept on a real or synthetic IoT sensor node project. Confirm 5+ concepts with correct structure (BOM, block diagram, scored concepts).
- D-10: This is a blocking gate. If regression detected, phase is NOT done.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Structural verification — zero JS fences, all headers, file size</name>
  <read_first>
    - skills/concept/SKILL.md (scan from line 1 to end — verify global state after all three rewrite plans)
  </read_first>
  <files>skills/concept/SKILL.md</files>
  <action>
Run automated checks to verify the complete rewrite before attempting live invocation.

Run these checks in sequence and report each result:

1. JS fence count:
   `grep -c '```javascript' skills/concept/SKILL.md`
   Expected: 0. Any non-zero result is a bug — identify which lines still have JS fences using `grep -n '```javascript' skills/concept/SKILL.md` and report them.

2. All phase headers present:
   `grep -c '^## PHASE 1:\|^## PHASE 2:\|^## PHASE 2\.5:\|^## PHASE 3:\|^## PHASE 4:\|^## PHASE 5:\|^## PHASE 6:\|^## PHASE 7:\|^## PHASE DISPATCH' skills/concept/SKILL.md`
   Expected: 9. Missing headers indicate a phase section was accidentally deleted.

3. File size:
   `wc -c skills/concept/SKILL.md`
   Expected: substantially less than 254330 bytes (original). Target ~35KB (35840 bytes). Report actual size.

4. Line count:
   `wc -l skills/concept/SKILL.md`
   Expected: substantially less than 7510 lines. Report actual count.

5. No orphaned JS close fences (closing ``` without matching opening javascript):
   `grep -n '^```$' skills/concept/SKILL.md | wc -l`
   Report the count. A large number here is a warning that close-fences from other code blocks may have been incorrectly removed — cross-check against non-JS fences (bash, yaml, etc.) that are legitimate.

If check 1 or check 2 fails, STOP and report. Do not proceed to Task 2. The failing plan(s) must be re-executed with targeted fixes before validation can continue.

If checks 1 and 2 pass, proceed to Task 2.
  </action>
  <verify>
    <automated>grep -c '```javascript' /home/william/repo/LibreSpin/LibreSpin/skills/concept/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '```javascript' skills/concept/SKILL.md` returns 0
    - `grep -c '^## PHASE 1:\|^## PHASE 2:\|^## PHASE 2\.5:\|^## PHASE 3:\|^## PHASE 4:\|^## PHASE 5:\|^## PHASE 6:\|^## PHASE 7:\|^## PHASE DISPATCH' skills/concept/SKILL.md` returns 9
    - `wc -c skills/concept/SKILL.md` reports a value less than 254330 (original size)
    - No regression report generated (all checks pass before Task 2 begins)
  </acceptance_criteria>
  <done>All structural checks pass: zero JS fences, 9 phase headers present, file size reduced. Ready for live invocation test.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Behavioral validation — run /librespin:concept on IoT sensor node project</name>
  <what-built>
The JS-to-prose rewrite of skills/concept/SKILL.md is structurally complete (Task 1 passed). This checkpoint tests behavioral fidelity: does the skill still produce correct output after the rewrite?
  </what-built>
  <how-to-verify>
Run the following in a fresh Claude Code window (clear context first to avoid stale skill cache):

1. Create a synthetic requirements file at `/tmp/iot-test-req.md` with this content:
   ```
   # IoT Soil Moisture Sensor Node

   ## Requirements
   - Measure soil moisture and temperature every 15 minutes
   - Transmit data via LoRa to a base station up to 2km away
   - Battery powered, target 12 months on 2x AA batteries
   - Waterproof enclosure, IP65 minimum
   - Cost target: under $15 per node BOM
   ```

2. Invoke the skill:
   `/librespin:concept --input /tmp/iot-test-req.md --depth medium`

3. Verify the output meets these criteria:
   - At least 5 distinct hardware concepts are generated
   - Each concept includes a BOM (component list with MPNs or part descriptions)
   - Each concept includes a block diagram (text-based or described)
   - Each concept has a score or ranking showing relative evaluation (the 50/30/20 scoring weights should be visible or implicit in the ranking)
   - Output files are saved to `.librespin/` directory structure

4. Check that the scoring criteria from Phase 1 are applied:
   - Critical requirements (score weight 50) are reflected in concept rankings
   - Concepts differ meaningfully from each other (diversity threshold enforced)

If the skill produces 5+ concepts with BOM, block diagram, and ranked output: type "approved".
If the skill produces fewer than 5 concepts, missing BOMs, missing block diagrams, or clearly wrong rankings: describe the regression.
  </how-to-verify>
  <resume-signal>Type "approved" if output is correct, or describe the specific regression observed</resume-signal>
</task>

</tasks>

<verification>
Phase 1000 complete when:
- `grep -c '```javascript' skills/concept/SKILL.md` returns 0
- All 9 phase headers present
- File substantially smaller than 254KB
- Human approved the /librespin:concept invocation output (5+ concepts, BOM, block diagram, scored)
</verification>

<success_criteria>
Zero JS fences remain in skills/concept/SKILL.md. All 9 phase headers intact. File size reduced from 254KB toward ~35KB target. Live /librespin:concept run on IoT sensor node project produces 5+ concepts with BOM, block diagram, and scored output. Human approval received.
</success_criteria>

<output>
After completion, create `.planning/phases/1000-concept-skill-js-to-prose/1000-04-SUMMARY.md`
</output>
