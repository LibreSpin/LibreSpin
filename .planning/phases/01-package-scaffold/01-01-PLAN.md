---
phase: 01-package-scaffold
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - .gitignore
  - pyproject.toml
  - bin/install.js
  - .claude/skills/librespin-concept/SKILL.md
  - .claude/agents/librespin-concept.md
  - .claude/librespin/templates/requirements.yaml
  - .claude/librespin/templates/concept-template.md
  - .claude/librespin/templates/overview-template.md
autonomous: true
requirements:
  - PKG-01
  - PKG-04

must_haves:
  truths:
    - "Running `node bin/install.js` completes without error and prints a restart instruction"
    - "After install, ~/.claude/skills/librespin-concept/, ~/.claude/agents/, and ~/.claude/librespin/templates/ directories exist with correct files"
    - "package.json has name librespin-install, version 0.1.0, MIT license, bin entry pointing to bin/install.js, files array with bin/ and .claude/"
    - "src/, .venv/, dist/ directories are removed from the repo"
    - "pyproject.toml is stripped to minimal placeholder"
  artifacts:
    - path: "package.json"
      provides: "npm package metadata with bin entry for npx distribution"
      contains: "librespin-install"
    - path: "bin/install.js"
      provides: "ESM installer script that copies .claude/ subtree to target"
      min_lines: 60
      exports: null
    - path: ".claude/skills/librespin-concept/SKILL.md"
      provides: "Skill orchestrator placeholder with valid frontmatter"
      contains: "librespin:concept"
    - path: ".claude/agents/librespin-concept.md"
      provides: "Worker agent placeholder with valid frontmatter"
      contains: "name: librespin-concept"
    - path: ".claude/librespin/templates/requirements.yaml"
      provides: "Requirements YAML template stub"
    - path: ".claude/librespin/templates/concept-template.md"
      provides: "Concept template stub"
    - path: ".claude/librespin/templates/overview-template.md"
      provides: "Overview template stub"
    - path: ".gitignore"
      provides: "Ignore rules for node_modules, .venv, dist, etc."
      contains: "node_modules"
    - path: "pyproject.toml"
      provides: "Stripped Python placeholder"
  key_links:
    - from: "package.json"
      to: "bin/install.js"
      via: "bin.librespin-install entry"
      pattern: '"librespin-install":\\s*"\\./bin/install\\.js"'
    - from: "bin/install.js"
      to: ".claude/skills/librespin-concept/"
      via: "cp() call copies skill directory"
      pattern: "skills.*librespin-concept"
    - from: "bin/install.js"
      to: ".claude/agents/librespin-concept.md"
      via: "cp() call copies flat agent file"
      pattern: "agents.*librespin-concept\\.md"
    - from: "bin/install.js"
      to: ".claude/librespin/templates/"
      via: "cp() call copies templates directory"
      pattern: "librespin.*templates"
    - from: "package.json"
      to: ".claude/"
      via: "files array includes .claude/ for npm pack"
      pattern: '"\\.claude/"'
---

<objective>
Create the complete npm package scaffold for LibreSpin: package.json, installer script, placeholder skill/agent/template files, .gitignore, and cleanup of dead Python scaffolding.

Purpose: Deliver a working `npx librespin-install` command that copies the correct directory skeleton to ~/.claude/, satisfying PKG-01 and PKG-04.
Output: All files needed for `npm pack` and `node bin/install.js` to work end-to-end.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-package-scaffold/01-CONTEXT.md
@.planning/phases/01-package-scaffold/01-RESEARCH.md

<interfaces>
<!-- Reference: hw-concept installer and package.json (canonical source for porting) -->

From /home/william/repo/hw-concept/package.json:
```json
{
  "name": "hw-concept",
  "version": "0.1.0",
  "type": "module",
  "bin": { "hw-concept-install": "./bin/install.js" },
  "files": ["bin/", ".claude/"],
  "engines": { "node": ">=18.0.0" },
  "license": "MIT",
  "dependencies": { "js-yaml": "^4.1.0" }
}
```

From /home/william/repo/hw-concept/bin/install.js:
```javascript
#!/usr/bin/env node
import { cp, mkdir } from 'node:fs/promises';
import { existsSync } from 'node:fs';
import { homedir } from 'node:os';
import { join, dirname } from 'node:path';
import { fileURLToPath } from 'node:url';

const __dirname = dirname(fileURLToPath(import.meta.url));
// Copies .claude/ subtree: agents/hw-concept/ (dir), commands/hw-concept.md (file), hw-concept/templates/ (dir)
// LibreSpin adaptation: skills/librespin-concept/ (dir), agents/librespin-concept.md (flat file), librespin/templates/ (dir)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cleanup dead Python scaffolding and create package config files</name>
  <files>package.json, .gitignore, pyproject.toml</files>
  <read_first>
    - /home/william/repo/LibreSpin/LibreSpin/pyproject.toml
    - /home/william/repo/hw-concept/package.json
  </read_first>
  <action>
**Step 1: Remove dead directories (per D-01)**

Delete these directories entirely:
- `src/` (dead Python package scaffolding)
- `.venv/` (Python virtual environment)
- `dist/` (Python build output)

Use `rm -rf src/ .venv/ dist/` from repo root.

**Step 2: Strip pyproject.toml (per D-02)**

Replace current pyproject.toml contents with this exact minimal placeholder:

```toml
[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "librespin"
version = "0.0.1"
license = { text = "MIT" }
requires-python = ">=3.10"
```

Remove: description, readme, keywords, classifiers, [project.urls] — all gone.

**Step 3: Create package.json (per D-07, D-08, PKG-04)**

Create `package.json` at repo root with this exact content:

```json
{
  "name": "librespin-install",
  "version": "0.1.0",
  "description": "Install LibreSpin hardware concept design skill pack for Claude Code",
  "type": "module",
  "bin": {
    "librespin-install": "./bin/install.js"
  },
  "files": [
    "bin/",
    ".claude/"
  ],
  "engines": {
    "node": ">=18.0.0"
  },
  "keywords": [
    "claude-code",
    "hardware",
    "pcb",
    "electronics",
    "concept-design",
    "librespin"
  ],
  "author": "LibreSpin Contributors",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/LibreSpin/LibreSpin"
  }
}
```

Note: Zero dependencies (per D-08). No `"dependencies"` key at all.

**Step 4: Create .gitignore (per D-03)**

Create `.gitignore` at repo root with these entries:

```
# Python
.venv/
dist/
*.egg-info/
__pycache__/
*.pyc

# Node
node_modules/

# OS
.DS_Store
Thumbs.db

# IDE
.idea/
.vscode/
*.swp
*.swo
```
  </action>
  <verify>
    <automated>cd /home/william/repo/LibreSpin/LibreSpin && test ! -d src && test ! -d .venv && test ! -d dist && echo "PASS: dead dirs removed" && node -e "const p=JSON.parse(require('fs').readFileSync('package.json','utf8')); if(p.name!=='librespin-install')throw new Error('wrong name'); if(p.bin['librespin-install']!=='./bin/install.js')throw new Error('wrong bin'); if(p.dependencies)throw new Error('has deps'); if(!p.files.includes('.claude/'))throw new Error('missing .claude in files'); console.log('PASS: package.json valid')" && grep -q 'node_modules' .gitignore && echo "PASS: gitignore has node_modules" && grep -c '^\[project\]' pyproject.toml | grep -q 1 && ! grep -q 'keywords' pyproject.toml && echo "PASS: pyproject.toml stripped"</automated>
  </verify>
  <acceptance_criteria>
    - `src/` directory does not exist
    - `.venv/` directory does not exist
    - `dist/` directory does not exist
    - `package.json` contains `"name": "librespin-install"`
    - `package.json` contains `"librespin-install": "./bin/install.js"` in bin
    - `package.json` does NOT contain a `"dependencies"` key
    - `package.json` contains `".claude/"` in files array
    - `package.json` contains `"type": "module"`
    - `.gitignore` contains `node_modules/`
    - `.gitignore` contains `.venv/`
    - `pyproject.toml` does NOT contain `keywords`
    - `pyproject.toml` does NOT contain `[project.urls]`
    - `pyproject.toml` contains `requires-python = ">=3.10"`
  </acceptance_criteria>
  <done>Dead Python scaffolding removed. package.json has correct metadata per PKG-04. pyproject.toml stripped to minimal placeholder per D-02. .gitignore covers both Node and Python artifacts per D-03.</done>
</task>

<task type="auto">
  <name>Task 2: Create placeholder skill, agent, and template files</name>
  <files>.claude/skills/librespin-concept/SKILL.md, .claude/agents/librespin-concept.md, .claude/librespin/templates/requirements.yaml, .claude/librespin/templates/concept-template.md, .claude/librespin/templates/overview-template.md</files>
  <read_first>
    - /home/william/repo/LibreSpin/LibreSpin/.planning/phases/01-package-scaffold/01-RESEARCH.md
  </read_first>
  <action>
Create the `.claude/` subtree in the repo root. These are the files that the installer will copy to `~/.claude/`. All files are minimal placeholders with valid structure — Phase 2 will fill real content.

**File 1: `.claude/skills/librespin-concept/SKILL.md` (per D-10, D-12)**

Create directories: `.claude/skills/librespin-concept/`

```markdown
---
description: LibreSpin hardware concept design workflow
argument-hint: "[--input FILE] [--depth quick|medium|thorough]"
allowed-tools:
  - Read
  - Write
  - Agent
  - AskUserQuestion
  - Glob
  - Bash
  - WebSearch
---

# /librespin:concept

TODO: Port from hw-concept in Phase 2.
```

**File 2: `.claude/agents/librespin-concept.md` (per D-09, D-12)**

Create directory: `.claude/agents/`

Note: This is a FLAT file, not a subdirectory. The file path is `.claude/agents/librespin-concept.md`.

```markdown
---
name: librespin-concept
description: Generate hardware concept designs with BOMs and block diagrams. Supports multi-phase workflow with state persistence.
tools: Read, Write, WebSearch, Grep, Glob, AskUserQuestion
color: blue
---

# LibreSpin Concept Generator Agent

TODO: Port from hw-concept in Phase 2.
```

**File 3: `.claude/librespin/templates/requirements.yaml` (per D-11, D-12)**

Create directories: `.claude/librespin/templates/`

```yaml
# LibreSpin Requirements Template
# TODO: Port from hw-concept in Phase 2
---
project_name: ""
requirements: []
```

**File 4: `.claude/librespin/templates/concept-template.md` (per D-11, D-12)**

```markdown
# Concept: {{concept_name}}

TODO: Port from hw-concept in Phase 2.
```

**File 5: `.claude/librespin/templates/overview-template.md` (per D-11, D-12)**

```markdown
# Project Overview: {{project_name}}

TODO: Port from hw-concept in Phase 2.
```
  </action>
  <verify>
    <automated>cd /home/william/repo/LibreSpin/LibreSpin && test -f .claude/skills/librespin-concept/SKILL.md && echo "PASS: SKILL.md exists" && test -f .claude/agents/librespin-concept.md && echo "PASS: agent file exists (flat)" && test -f .claude/librespin/templates/requirements.yaml && test -f .claude/librespin/templates/concept-template.md && test -f .claude/librespin/templates/overview-template.md && echo "PASS: all 3 templates exist" && grep -q "allowed-tools" .claude/skills/librespin-concept/SKILL.md && echo "PASS: SKILL.md has frontmatter" && grep -q "name: librespin-concept" .claude/agents/librespin-concept.md && echo "PASS: agent has name frontmatter"</automated>
  </verify>
  <acceptance_criteria>
    - `.claude/skills/librespin-concept/SKILL.md` exists and contains `description: LibreSpin hardware concept design workflow`
    - `.claude/skills/librespin-concept/SKILL.md` contains `Agent` in allowed-tools (not `Task`)
    - `.claude/agents/librespin-concept.md` exists as a flat file (not `.claude/agents/librespin-concept/AGENT.md`)
    - `.claude/agents/librespin-concept.md` contains `name: librespin-concept` in frontmatter
    - `.claude/agents/librespin-concept.md` contains `tools: Read, Write, WebSearch, Grep, Glob, AskUserQuestion`
    - `.claude/agents/librespin-concept.md` contains `color: blue`
    - `.claude/librespin/templates/requirements.yaml` exists and contains `project_name`
    - `.claude/librespin/templates/concept-template.md` exists
    - `.claude/librespin/templates/overview-template.md` exists
    - No file contains the string `hw-concept`
  </acceptance_criteria>
  <done>All placeholder files created with valid frontmatter and structure. Skill uses `Agent` tool (not deprecated `Task`). Agent is a flat file per D-09. Templates are YAML/markdown stubs per D-11. All ready for Phase 2 content port.</done>
</task>

<task type="auto">
  <name>Task 3: Create installer script and verify end-to-end</name>
  <files>bin/install.js</files>
  <read_first>
    - /home/william/repo/hw-concept/bin/install.js
    - /home/william/repo/LibreSpin/LibreSpin/package.json
    - /home/william/repo/LibreSpin/LibreSpin/.claude/skills/librespin-concept/SKILL.md
    - /home/william/repo/LibreSpin/LibreSpin/.claude/agents/librespin-concept.md
  </read_first>
  <action>
Create directory: `bin/`

Create `bin/install.js` — an ESM installer script adapted from hw-concept. The file must:

1. Start with `#!/usr/bin/env node` shebang (line 1, critical for npx)
2. Use ESM imports: `cp`, `mkdir` from `node:fs/promises`; `existsSync` from `node:fs`; `homedir` from `node:os`; `join`, `dirname` from `node:path`; `fileURLToPath` from `node:url`
3. Compute `__dirname` via `dirname(fileURLToPath(import.meta.url))`
4. Parse `--local` and `--help`/`-h` from `process.argv.slice(2)`

**Help text (per D-05):**
```
Usage: npx librespin-install [options]

Install LibreSpin skill pack for Claude Code.

Options:
  --local    Install to current directory (.claude/) instead of home (~/.claude/)
  --help     Show this help message

Examples:
  npx librespin-install           # Install to ~/.claude/
  npx librespin-install --local   # Install to ./.claude/
```

**Install function logic:**

Set `targetBase` to `join(process.cwd(), '.claude')` if `--local`, else `join(homedir(), '.claude')`.
Set `sourceBase` to `join(__dirname, '..', '.claude')`.

Print: `Installing LibreSpin to ${targetBase}...\n`

Step A — Create target directories (per Pitfall 4 in research — mkdir BEFORE cp):
```javascript
const dirs = [
  'skills/librespin-concept',
  'agents',
  'librespin/templates',
];
for (const dir of dirs) {
  await mkdir(join(targetBase, dir), { recursive: true });
}
```

Step B — Copy skill directory (recursive, force per D-04):
```javascript
await cp(
  join(sourceBase, 'skills', 'librespin-concept'),
  join(targetBase, 'skills', 'librespin-concept'),
  { recursive: true, force: true }
);
console.log('  ✓ Skill installed');
```

Step C — Copy agent flat file (NO recursive — per Pitfall 1 in research, this is a file not a directory):
```javascript
await cp(
  join(sourceBase, 'agents', 'librespin-concept.md'),
  join(targetBase, 'agents', 'librespin-concept.md'),
  { force: true }
);
console.log('  ✓ Agent installed');
```

Step D — Copy templates directory (recursive, force):
```javascript
await cp(
  join(sourceBase, 'librespin', 'templates'),
  join(targetBase, 'librespin', 'templates'),
  { recursive: true, force: true }
);
console.log('  ✓ Templates installed');
```

Step E — Print completion message (per PKG-01 success criterion 1):
```javascript
console.log(`\n✓ Installation complete!`);
console.log(`\nFiles installed to: ${targetBase}`);
if (!isLocal) {
  console.log('\nRestart Claude Code to activate /librespin:concept skill.');
}
```

Wrap steps A-E in try/catch. On error: `console.error('\n✗ Installation failed:', err.message); process.exit(1);`

**After creating the file**, run `chmod +x bin/install.js` to make it executable.

**End-to-end test:** Run `node bin/install.js --local` from a temporary directory to verify the installer works. Then verify the created files exist. Then clean up.
  </action>
  <verify>
    <automated>cd /home/william/repo/LibreSpin/LibreSpin && head -1 bin/install.js | grep -q '#!/usr/bin/env node' && echo "PASS: shebang present" && grep -q 'librespin-concept' bin/install.js && echo "PASS: references librespin-concept" && grep -q 'force: true' bin/install.js && echo "PASS: force overwrite" && grep -q '\-\-local' bin/install.js && echo "PASS: --local flag" && grep -q 'Restart Claude Code' bin/install.js && echo "PASS: restart message" && TMPDIR=$(mktemp -d) && cd "$TMPDIR" && node /home/william/repo/LibreSpin/LibreSpin/bin/install.js --local && test -f .claude/skills/librespin-concept/SKILL.md && test -f .claude/agents/librespin-concept.md && test -f .claude/librespin/templates/requirements.yaml && echo "PASS: --local install creates all expected files" && rm -rf "$TMPDIR"</automated>
  </verify>
  <acceptance_criteria>
    - `bin/install.js` first line is exactly `#!/usr/bin/env node`
    - `bin/install.js` contains `import { cp, mkdir } from 'node:fs/promises'`
    - `bin/install.js` contains `'skills/librespin-concept'` in dirs array
    - `bin/install.js` contains `agents/librespin-concept.md` (flat file copy, not directory)
    - `bin/install.js` contains `{ force: true }` (per D-04 silent overwrite)
    - `bin/install.js` contains `--local` flag handling (per D-05)
    - `bin/install.js` contains `Restart Claude Code to activate /librespin:concept skill.`
    - `bin/install.js` does NOT contain the string `hw-concept`
    - `bin/install.js` does NOT contain `uninstall` (per D-06, deferred to Phase 4)
    - Running `node bin/install.js --local` from a temp directory creates `.claude/skills/librespin-concept/SKILL.md`, `.claude/agents/librespin-concept.md`, and `.claude/librespin/templates/requirements.yaml`
    - `bin/install.js` is executable (`chmod +x`)
  </acceptance_criteria>
  <done>Installer script works end-to-end. `node bin/install.js --local` copies all 5 placeholder files to the correct directory structure. Help text displays on `--help`. Restart message prints on global install. No hw-concept references remain.</done>
</task>

</tasks>

<verification>
1. `npm pack --dry-run` from repo root shows only `bin/` and `.claude/` files in the tarball (no `src/`, `.venv/`, `dist/`, `.planning/`, `.git/`)
2. `node bin/install.js --local` in a temp directory creates all 3 target directories with all 5 files
3. `grep -r "hw-concept" package.json bin/ .claude/` returns zero matches
4. `node -e "JSON.parse(require('fs').readFileSync('package.json'))"` parses without error
5. `node bin/install.js --help` prints usage without error
</verification>

<success_criteria>
- PKG-01: `node bin/install.js` completes without error and prints restart instruction
- PKG-04: package.json has name `librespin-install`, version `0.1.0`, MIT license, correct bin entry, `files` includes `bin/` and `.claude/`
- D-01: `src/`, `.venv/`, `dist/` removed
- D-02: pyproject.toml stripped to 4-field placeholder
- D-03: .gitignore covers node_modules, .venv, dist
- D-04: Installer uses `force: true` for idempotent overwrite
- D-05: `--local` flag works for per-project install
- D-07: Package name is `librespin-install`
- D-08: Zero npm dependencies
- D-09: Agent is flat file at `agents/librespin-concept.md`
- D-10: Skill at `skills/librespin-concept/SKILL.md`
- D-11: Templates at `librespin/templates/`
- D-12: All placeholder files have valid frontmatter/structure
</success_criteria>

<output>
After completion, create `.planning/phases/01-package-scaffold/01-01-SUMMARY.md`
</output>
