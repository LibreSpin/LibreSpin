---
phase: 06-calcpad-ce-skill
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/artifacts/pr-001-calcpadservice-authsettings-fix.md
  - .planning/artifacts/pr-002-linux-build-documentation.md
autonomous: true
requirements: [CALC-01]
must_haves:
  truths:
    - "Two upstream PR artifacts exist as markdown files ready to paste into GitHub"
    - "PR 001 contains the AuthSettings fix diff and a complete PR description (title, summary, test plan)"
    - "PR 002 contains Linux build documentation updates (binary name, port anomaly, dotnet-install path)"
    - "Both artifacts are submittable without modification — user can paste directly into github.com PR form"
  artifacts:
    - path: ".planning/artifacts/pr-001-calcpadservice-authsettings-fix.md"
      provides: "PR body + unified diff for CalcpadService.cs AuthSettings fix"
      contains: "CalcpadService.cs"
      min_lines: 40
    - path: ".planning/artifacts/pr-002-linux-build-documentation.md"
      provides: "PR body + README/docs diff for Linux build notes"
      contains: "Cli"
      min_lines: 40
  key_links:
    - from: "LibreSpin/CalcpadCE (fork)"
      to: "imartincei/CalcpadCE (upstream)"
      via: "GitHub PR form, manually submitted by user"
      pattern: "imartincei/CalcpadCE"
---

<objective>
Produce the two upstream-contribution PR artifacts described in D-16/D-17/D-18 — markdown files containing PR titles, descriptions, and unified diffs that the user can copy-paste into the GitHub PR form against `imartincei/CalcpadCE` when ready.

Purpose: Goodwill contribution to upstream CalcpadCE. These are artifacts only — not auto-submitted. Phase 6 ships independently of upstream acceptance.
Output: Two markdown files in `.planning/artifacts/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-calcpad-ce-skill/06-CONTEXT.md
@.planning/phases/06-calcpad-ce-skill/06-RESEARCH.md
@.planning/spike-calcpad.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write pr-001-calcpadservice-authsettings-fix.md</name>
  <read_first>
    - .planning/spike-calcpad.md (for the exact CalcpadService.cs symptom and fix)
    - .planning/phases/06-calcpad-ce-skill/06-CONTEXT.md (D-16 — PR content scope)
  </read_first>
  <files>.planning/artifacts/pr-001-calcpadservice-authsettings-fix.md</files>
  <action>
    Create `.planning/artifacts/pr-001-calcpadservice-authsettings-fix.md` with this structure (fill in the diff body from the spike notes — the exact CalcpadService.cs lines that were deleted):

    ```markdown
    # PR 001: fix(Calcpad.Server): remove references to deleted MacroParser.AuthSettings API

    **Target:** imartincei/CalcpadCE — main branch
    **Source:** LibreSpin/CalcpadCE — librespin-patched branch
    **Type:** Bug fix (build-breaking)

    ## Summary

    `Calcpad.Server/Services/CalcpadService.cs` references `MacroParser.AuthSettings`, a member that was removed from the `Calcpad.Core` parser in recent development commits. This causes CS1061 / CS0234 build errors when building from source against the current parser on .NET 10.

    This PR removes the stale `AuthSettings` references so `Calcpad.Server` builds cleanly on .NET 10.

    ## Reproduction

    On a clean Linux environment with .NET SDK 10:
    ```bash
    git clone https://github.com/imartincei/CalcpadCE
    cd CalcpadCE
    dotnet build Calcpad.Server/Calcpad.Server.csproj
    ```
    Observed: CS1061 errors on `MacroParser.AuthSettings`.

    ## Fix

    Delete the optional AuthSettings wiring block in `CalcpadService.cs`. The parser no longer exposes this surface; the server side should not assume it.

    ## Diff

    (Full unified diff from .planning/artifacts/calcpadce-authsettings-patch.diff — inline it here verbatim.)

    ```diff
    <PASTE DIFF FROM .planning/artifacts/calcpadce-authsettings-patch.diff HERE>
    ```

    ## Test plan

    - [x] `dotnet build Calcpad.Server/Calcpad.Server.csproj` succeeds on .NET 10
    - [x] `dotnet publish Calcpad.Cli -r linux-x64 --self-contained true -p:PublishSingleFile=true` produces a working binary
    - [x] `Calcpad.Server --urls http://localhost:9421` starts and accepts `POST /api/calcpad/convert`
    - [x] Verified end-to-end in LibreSpin Phase 5 spike (see LibreSpin/LibreSpin .planning/spike-calcpad.md)

    ## Related

    Surfaced while building binaries for the LibreSpin project (https://github.com/LibreSpin/LibreSpin), which uses CalcPad CE as a headless calculation engine.
    ```

    After writing, read `.planning/artifacts/calcpadce-authsettings-patch.diff` (created in Plan 01 Task 2) and substitute the `<PASTE DIFF ... HERE>` placeholder with its actual contents. If that file doesn't exist yet (Plan 01 not run), leave a TODO marker: `<!-- TODO: inline diff once 06-01 produces calcpadce-authsettings-patch.diff -->` and the task is still considered done — the artifact is complete once Plan 01 merges.
  </action>
  <verify>
    <automated>test -f .planning/artifacts/pr-001-calcpadservice-authsettings-fix.md && grep -q "CalcpadService.cs" .planning/artifacts/pr-001-calcpadservice-authsettings-fix.md && grep -q "imartincei/CalcpadCE" .planning/artifacts/pr-001-calcpadservice-authsettings-fix.md && grep -q "AuthSettings" .planning/artifacts/pr-001-calcpadservice-authsettings-fix.md && [ $(wc -l < .planning/artifacts/pr-001-calcpadservice-authsettings-fix.md) -ge 40 ]</automated>
  </verify>
  <acceptance_criteria>
    - File exists at the specified path, ≥40 lines
    - Contains literal strings: `CalcpadService.cs`, `AuthSettings`, `imartincei/CalcpadCE`, `## Test plan`, `## Diff`, `CS1061`
    - Contains either the inlined diff (if Plan 01 ran first) or the TODO marker
  </acceptance_criteria>
  <done>PR 001 artifact is ready to paste into GitHub (minus the diff inline if Plan 01 hasn't run yet).</done>
</task>

<task type="auto">
  <name>Task 2: Write pr-002-linux-build-documentation.md</name>
  <read_first>
    - .planning/spike-calcpad.md (Linux build path deviations, binary naming, port anomaly)
    - .planning/phases/06-calcpad-ce-skill/06-CONTEXT.md (D-16 #2)
    - .planning/phases/06-calcpad-ce-skill/06-RESEARCH.md (State of the Art table — the confirmed realities)
  </read_first>
  <files>.planning/artifacts/pr-002-linux-build-documentation.md</files>
  <action>
    Create `.planning/artifacts/pr-002-linux-build-documentation.md` with this structure:

    ```markdown
    # PR 002: docs: document Linux build notes (binary naming, server port, dotnet-install path)

    **Target:** imartincei/CalcpadCE — main branch
    **Source:** LibreSpin/CalcpadCE — librespin-patched branch
    **Type:** Documentation

    ## Summary

    While building CalcPad CE on Linux for the LibreSpin project, three Linux-specific details were not documented in the README. This PR adds a "Building on Linux" section to the README capturing them so future Linux contributors don't re-discover them.

    ## Notes added

    1. **Binary assembly name** — On Linux, the published `Calcpad.Cli` project produces an ELF binary named `Cli`, not `Calcpad.Cli`. This is the default dotnet assembly name from the csproj. Users invoking the binary must call `./Cli`, not `./Calcpad.Cli`.

    2. **Calcpad.Server port is not 8080 by default** — When started without `--urls`, Kestrel binds a non-deterministic port (observed 9420 in one run). Scripts that assume port 8080 will fail. Recommended invocation:
       ```
       Calcpad.Server --urls http://localhost:9421
       ```
       Or parse the `Now listening on:` line from stdout at startup.

    3. **.NET 10 install path without sudo** — `dotnet-install.sh --install-dir /tmp/dotnet10 --version 10.0.x` works in sandboxed environments (e.g. CI or agent containers) without requiring root. The resulting self-contained publish does NOT need .NET at runtime, so end-users don't need this step.

    ## Proposed README diff

    (Append a new section to README.md under "Building from source".)

    ```diff
    --- a/README.md
    +++ b/README.md
    @@ -X,Y +X,Y @@
     ## Building from source

     ### Windows / macOS
     ...

    +### Linux (x64)
    +
    +```bash
    +# Install .NET 10 SDK (no sudo required)
    +curl -sSL https://dot.net/v1/dotnet-install.sh | bash -s -- \
    +  --install-dir /tmp/dotnet10 --version 10.0.x
    +export PATH=/tmp/dotnet10:$PATH
    +
    +# Publish self-contained single-file Cli
    +dotnet publish Calcpad.Cli/Calcpad.Cli.csproj \
    +  -r linux-x64 --self-contained true \
    +  -p:PublishSingleFile=true -c Release -o ./dist
    +
    +# Note: the ELF binary is named `Cli`, not `Calcpad.Cli`
    +./dist/Cli input.cpd output.html -s
    +```
    +
    +### Calcpad.Server port
    +
    +Kestrel does not default to port 8080 — always pass an explicit URL:
    +
    +```bash
    +Calcpad.Server --urls http://localhost:9421
    +```
    +
    +Or parse the `Now listening on:` line from stdout if you need the actual bound port.
    ```

    ## Test plan

    - [x] Copy-paste build commands work on fresh Ubuntu 22.04 (verified in LibreSpin Phase 5 spike)
    - [x] Binary `Cli` executes and produces HTML output
    - [x] `Calcpad.Server --urls http://localhost:9421` binds correctly
    - [x] `POST /api/calcpad/convert` returns evaluated HTML

    ## Related

    Documented while building pre-release binaries for LibreSpin (https://github.com/LibreSpin/LibreSpin). Evidence in LibreSpin's .planning/spike-calcpad.md.
    ```
  </action>
  <verify>
    <automated>test -f .planning/artifacts/pr-002-linux-build-documentation.md && grep -q "localhost:9421" .planning/artifacts/pr-002-linux-build-documentation.md && grep -q "dotnet-install.sh" .planning/artifacts/pr-002-linux-build-documentation.md && grep -q "named .Cli." .planning/artifacts/pr-002-linux-build-documentation.md && grep -q "imartincei/CalcpadCE" .planning/artifacts/pr-002-linux-build-documentation.md && [ $(wc -l < .planning/artifacts/pr-002-linux-build-documentation.md) -ge 40 ]</automated>
  </verify>
  <acceptance_criteria>
    - File exists, ≥40 lines
    - Contains literal strings: `localhost:9421`, `dotnet-install.sh`, `imartincei/CalcpadCE`, `linux-x64`, `--self-contained`, `## Test plan`
    - Does NOT contain `localhost:8080` as a recommended value
    - Contains the note that the Linux binary is named `Cli` (not `Calcpad.Cli`)
  </acceptance_criteria>
  <done>PR 002 artifact is ready to paste into GitHub.</done>
</task>

</tasks>

<verification>
- Both PR artifact files exist in .planning/artifacts/
- Each file contains a complete PR body (title, summary, diff, test plan, related links)
- No auto-submission — per D-17, user submits manually
</verification>

<success_criteria>
When the user is ready to contribute upstream, they open the GitHub PR form against `imartincei/CalcpadCE` and copy-paste the contents of each artifact without modification.
</success_criteria>

<output>
Create `.planning/phases/06-calcpad-ce-skill/06-03-SUMMARY.md` listing both artifact paths, a 1-line description of each PR, and a note to the user about when/how to submit them.
</output>
