---
phase: 05-calcpad-ce-spike
plan: 03
type: execute
wave: 3
depends_on: ["05-01", "05-02"]
files_modified:
  - .planning/spike-calcpad.md
autonomous: false
requirements: []
must_haves:
  truths:
    - "A spike report exists at .planning/spike-calcpad.md (per D-08, NOT .librespin/)"
    - "Report records confirmed binary names and invocation paths for both Calcpad.Cli and Calcpad.Server"
    - "Report documents the .NET 10 install path used (apt: dotnet-sdk-10.0)"
    - "Report records explicit go/no-go verdict per D-04..D-07 criteria"
    - "Report includes a recommended Phase 6 implementation strategy (CLI-first, REST-first, or replan)"
    - "User has reviewed and approved the spike findings before Phase 6 begins"
  artifacts:
    - path: ".planning/spike-calcpad.md"
      provides: "Phase 6 planning input — go/no-go decision and recommended implementation path"
      contains: "Verdict: GO|NO-GO"
  key_links:
    - from: ".planning/spike-calcpad.md"
      to: "Phase 6 plan-phase"
      via: "Phase 6 planner reads this file to choose CALC-01 / CALC-08 implementation path"
      pattern: "spike-calcpad\\.md"
---

<objective>
Synthesize the build evidence (plan 01) and test evidence (plan 02) into a single decision document at `.planning/spike-calcpad.md`, then pause for user review and approval before Phase 6 planning begins.

Purpose: Phase 5 exists solely to produce this artifact. Phase 6's implementation strategy for CALC-01 (CLI prereq check) and CALC-08 (REST API fallback) depends entirely on what this report says about which paths actually work on this system.

Output: `.planning/spike-calcpad.md` (the single Phase 5 deliverable per D-08) plus user sign-off via a human-verify checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-calcpad-ce-spike/05-CONTEXT.md
@.planning/phases/05-calcpad-ce-spike/05-RESEARCH.md
@.planning/phases/05-calcpad-ce-spike/05-01-SUMMARY.md
@.planning/phases/05-calcpad-ce-spike/05-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write spike-calcpad.md decision report</name>
  <files>
    - .planning/spike-calcpad.md
  </files>
  <read_first>
    - .planning/phases/05-calcpad-ce-spike/05-CONTEXT.md          # D-04..D-08 go/no-go criteria and artifact location decision
    - .planning/phases/05-calcpad-ce-spike/05-RESEARCH.md         # for "expected vs observed" comparison
    - .planning/phases/05-calcpad-ce-spike/05-01-SUMMARY.md       # build results
    - .planning/phases/05-calcpad-ce-spike/05-02-SUMMARY.md       # CLI + REST test results
    - /tmp/spike-calcpad/cli-stdout.log                            # raw CLI evidence
    - /tmp/spike-calcpad/server-stdout.log                         # raw server evidence
    - /tmp/spike-calcpad/rest-headers.txt                          # raw REST evidence
  </read_first>
  <action>
    Per D-08, write the spike report to `.planning/spike-calcpad.md` (NOT `.librespin/spike-calcpad.md` — the roadmap text is overridden). The .librespin/ directory is reserved for project-level concept output; spike findings are a planning artifact.

    Use this exact section structure (every section is required — leave a section's content as "N/A — see notes" only if literally not applicable, never omit a header):

    ```markdown
    # Phase 5 Spike: CalcPad CE on Linux

    **Date:** {YYYY-MM-DD of run}
    **Verdict:** {GO | NO-GO}
    **Recommended Phase 6 path:** {CLI-first | REST-first | both | replan}

    ## 1. Environment

    - OS: {uname -a output}
    - .NET SDK installed: {dotnet --list-sdks output}
    - .NET runtimes available: {dotnet --list-runtimes output}
    - CalcpadCE source: github.com/imartincei/CalcpadCE @ {git rev}

    ## 2. Build Results

    | Component | Built? | Path | Size | Target Framework |
    |-----------|--------|------|------|------------------|
    | Calcpad.Cli | yes/no | /tmp/calcpad-cli-out/Calcpad.Cli | {MB} | {netX.0} |
    | Calcpad.Server | yes/no | {actual path} | {MB} | {netX.0} |

    ## 3. CLI Headless Test (D-04 — primary go/no-go gate)

    - Command: `Calcpad.Cli /tmp/spike-calcpad/test.cpd /tmp/spike-calcpad/cli-output.html -s`
    - Exit code: {N}
    - Output file produced: yes/no, {bytes}
    - First 10 lines of output: {paste}
    - **CLI verdict:** {WORKING | BROKEN}

    ## 4. REST API Test (D-03 — CALC-08 path validation)

    - Server start command: {command}
    - Bound port: {N}
    - Server target framework: {netX.0}
    - Request: `POST http://localhost:{port}/api/calcpad/convert`
    - HTTP status: {code}
    - Response body size: {bytes}
    - **REST verdict:** {WORKING | BROKEN}

    ## 5. Go/No-Go Decision (per D-04..D-07)

    Apply the decision matrix:
    - CLI working → GO (D-04)
    - CLI broken, REST working → GO, Phase 6 must use REST-only path (D-06)
    - Neither working → NO-GO, Phase 6 must be replanned (D-07)

    **Verdict:** {GO | NO-GO}
    **Reason:** {one sentence}

    ## 6. Recommended Phase 6 Implementation Strategy

    - CALC-01 (prereq check): {what to check — e.g., "verify Calcpad.Cli exists at expected path; offer apt install dotnet-sdk-10.0 + clone+build instructions if absent"}
    - CALC-08 (REST fallback): {whether to implement, and how to start the server}
    - Binary distribution: {self-contained build vs runtime-dependent}
    - Known anomalies Phase 6 must handle: {list}

    ## 7. Deviations from Research

    {Any place RESEARCH.md predicted X but reality was Y — port number, target framework, build command, etc. Empty list if none.}

    ## 8. Evidence Files

    - /tmp/spike-calcpad/test.cpd
    - /tmp/spike-calcpad/cli-output.html
    - /tmp/spike-calcpad/cli-stdout.log
    - /tmp/spike-calcpad/server-stdout.log
    - /tmp/spike-calcpad/rest-response.html
    - /tmp/spike-calcpad/rest-headers.txt
    ```

    Fill every placeholder with actual observed values from the SUMMARYs and /tmp/spike-calcpad/ logs. Do NOT speculate — if a test was not run or a value not observed, write "not observed".
  </action>
  <verify>
    <automated>test -f .planning/spike-calcpad.md && grep -qE '^\*\*Verdict:\*\* (GO|NO-GO)' .planning/spike-calcpad.md && grep -q '## 1. Environment' .planning/spike-calcpad.md && grep -q '## 5. Go/No-Go Decision' .planning/spike-calcpad.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/spike-calcpad.md` exists
    - File contains a `**Verdict:**` line with value `GO` or `NO-GO`
    - File contains all 8 required section headers (`## 1.` through `## 8.`)
    - File contains either `dotnet-sdk-10.0` or the exact `dotnet --list-sdks` output (proof of .NET 10 install path documentation per ROADMAP success criterion 3)
    - File contains the literal binary name `Calcpad.Cli` (per ROADMAP success criterion 1)
    - File contains the literal endpoint `/api/calcpad/convert` (per ROADMAP success criterion 4)
    - File does NOT contain any TODO/PLACEHOLDER/{...} markers (all values filled)
    - File is at `.planning/spike-calcpad.md` and NOT at `.librespin/spike-calcpad.md` (per D-08)
  </acceptance_criteria>
  <done>
    Spike report written at the D-08 location with verdict, recommended Phase 6 strategy, and complete evidence trail.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: User reviews spike report and approves Phase 6 path</name>
  <files>
    - .planning/spike-calcpad.md
  </files>
  <read_first>
    - .planning/spike-calcpad.md
  </read_first>
  <action>
    Pause and present the spike report to the user for review. Do NOT proceed to Phase 6 planning until the user explicitly approves.

    Display:
    1. The full contents of `.planning/spike-calcpad.md` (use Read tool, show to user).
    2. A one-line summary: "Verdict: {GO|NO-GO}, Recommended Phase 6 path: {path}".
    3. The list of evidence files in `/tmp/spike-calcpad/` so the user can spot-check them.
    4. The exact resume-signal prompt below.

    Wait for user response. Acceptable approval signals: "approved", "ok", "go", "lgtm". Anything else → treat as a request for revision and route back to plan 03 task 1 (or escalate to user for direction).
  </action>
  <what-built>
    `.planning/spike-calcpad.md` — full Phase 5 deliverable. Contains environment details, build results, CLI test result, REST test result, GO/NO-GO verdict, and recommended Phase 6 implementation strategy (CLI-first vs REST-first vs replan).

    Evidence files in `/tmp/spike-calcpad/` (test.cpd, cli-output.html, cli-stdout.log, server-stdout.log, rest-response.html, rest-headers.txt) back the report's claims.
  </what-built>
  <how-to-verify>
    1. Read `.planning/spike-calcpad.md` end-to-end.
    2. Confirm the **Verdict** matches your expectation given the evidence (GO or NO-GO).
    3. Spot-check at least one evidence file in `/tmp/spike-calcpad/` (e.g., `head /tmp/spike-calcpad/cli-output.html` if CLI verdict is WORKING; `cat /tmp/spike-calcpad/rest-headers.txt` if REST verdict is WORKING).
    4. Confirm the "Recommended Phase 6 Implementation Strategy" section is concrete enough that Phase 6 planning can proceed (CALC-01 and CALC-08 paths are unambiguous).
    5. If GO: approve to move to Phase 6 planning.
       If NO-GO: review the failure mode and decide whether to retry the spike, change scope, or replan v0.2.
  </how-to-verify>
  <verify>
    <automated>test -f .planning/spike-calcpad.md</automated>
  </verify>
  <acceptance_criteria>
    - User has explicitly approved (resume-signal received)
    - `.planning/spike-calcpad.md` is unchanged after approval (or revised per user feedback and re-approved)
  </acceptance_criteria>
  <done>
    User has signed off on the spike findings and Phase 6 planning is unblocked.
  </done>
  <resume-signal>
    Type "approved" to confirm spike findings and unblock Phase 6 planning.
    Or describe what's missing / needs revision.
  </resume-signal>
</task>

</tasks>

<verification>
- `.planning/spike-calcpad.md` exists with all 8 required sections
- Report contains GO or NO-GO verdict
- Report references the actual binary names, install commands, and REST endpoint
- User has typed "approved" (or equivalent) on the checkpoint
</verification>

<success_criteria>
Phase 5 deliverable is on disk at the D-08 location, signed off by the user, and contains a recommended Phase 6 implementation path that the next plan-phase invocation can act on directly.
</success_criteria>

<output>
After completion, create `.planning/phases/05-calcpad-ce-spike/05-03-SUMMARY.md` recording:
- Final verdict (GO/NO-GO)
- Recommended Phase 6 path
- Link to .planning/spike-calcpad.md
- User approval timestamp / message
</output>
