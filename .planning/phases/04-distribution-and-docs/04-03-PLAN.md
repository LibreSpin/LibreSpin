---
phase: 04-distribution-and-docs
plan: 03
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - README.md
requirements:
  - PKG-03
autonomous: true
must_haves:
  truths:
    - "README.md tells a first-time user how to install via plugin marketplace (primary)"
    - "README.md documents the npx install path as a secondary option"
    - "README.md lists prerequisites (Claude Code + Node.js >= 18)"
    - "README.md shows the /librespin:concept first-run command and what to expect"
    - "README.md documents the uninstall command"
  artifacts:
    - path: "README.md"
      provides: "Quick-start documentation, ~40-60 lines"
      contains: "/plugin marketplace add LibreSpin/LibreSpin"
  key_links:
    - from: "README.md"
      to: "/librespin:concept skill command"
      via: "Quick Start section"
      pattern: "/librespin:concept"
---

<objective>
Replace the 5-line placeholder README.md with a tight quick-start (~40-60 lines) so a stranger arriving from the plugin marketplace can install LibreSpin, run the concept skill, and uninstall it without reading any other doc.

Purpose: Satisfies PKG-03 — README with quick-start instructions (install, first run, what to expect).

Output: New README.md at the repo root, replacing the placeholder.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-distribution-and-docs/04-CONTEXT.md
@.planning/phases/04-distribution-and-docs/04-RESEARCH.md
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace README.md with quick-start documentation</name>
  <files>README.md</files>
  <read_first>
    - README.md (current placeholder — 5 lines)
    - .planning/phases/04-distribution-and-docs/04-CONTEXT.md (D-09, D-10)
    - .planning/phases/04-distribution-and-docs/04-RESEARCH.md (Code Examples → README skeleton)
  </read_first>
  <action>
    Replace the entire contents of `README.md` at the repo root with the following markdown. Do not add config options, troubleshooting, architecture notes, or contributor docs (D-10 — those are v2). Target length: 40-60 lines.

    ```markdown
    # LibreSpin

    AI-driven hardware concept design for Claude Code. Describe your circuit in plain English and LibreSpin generates 4-6 fully evaluated architecture concepts — complete with bills of materials, block diagrams, and a side-by-side comparison matrix.

    **Status:** Pre-alpha. The concept workflow is functional; SPICE simulation, KiCad integration, and production export are on the v2+ roadmap.

    ## Install

    **Via Claude Code plugin marketplace (recommended)**

    ```
    /plugin marketplace add LibreSpin/LibreSpin
    /plugin install librespin
    ```

    **Via npx (also installs YAML templates)**

    ```
    npx librespin-install
    ```

    Use `--local` to install into the current project's `.claude/` instead of `~/.claude/`.

    ## Prerequisites

    - [Claude Code](https://claude.ai/code)
    - Node.js >= 18 (only required for the npx installer)

    ## Quick Start

    After installing, restart Claude Code, open any project, and run:

    ```
    /librespin:concept
    ```

    LibreSpin will ask you a series of questions about your hardware requirements (target use case, power budget, connectivity, environment, cost), then drive a 9-phase workflow:

    1. Requirements gathering and completeness scoring
    2. Architecture concept generation (4-6 diverse approaches)
    3. Concept validation against confidence thresholds
    4. Component research with real part numbers
    5. Detailed block diagrams with MPNs
    6. Self-critique and refinement
    7. Quality scoring
    8. Cross-concept comparison
    9. Recommendation with comparison matrix

    Expect the full workflow to take roughly 5-15 minutes depending on complexity. All output is written to `.librespin/` in your project.

    ## Uninstall

    ```
    npx librespin-install --uninstall
    ```

    Add `--local` if you installed locally.

    ## License

    MIT
    ```

    Notes for the executor:
    - The fenced code blocks containing `/plugin ...`, `npx ...`, and `/librespin:concept` use triple-backticks with no language tag — preserve them as-is.
    - The numbered list of 9 phases must match the workflow names used in SKILL.md (requirements, concept generation, validation, BOM research, block diagrams, refinement, scoring, comparison matrix). The exact phase numbering above is acceptable.
    - File length should land between 40 and 80 lines including blank lines.
  </action>
  <verify>
    <automated>test -f README.md && lines=$(wc -l < README.md) && [ "$lines" -ge 40 ] && [ "$lines" -le 80 ] && grep -q '/plugin marketplace add LibreSpin/LibreSpin' README.md && grep -q '/plugin install librespin' README.md && grep -q 'npx librespin-install' README.md && grep -q 'npx librespin-install --uninstall' README.md && grep -q '/librespin:concept' README.md && grep -q 'Node.js >= 18' README.md && grep -q 'Claude Code' README.md && grep -q '## Install' README.md && grep -q '## Prerequisites' README.md && grep -q '## Quick Start' README.md && grep -q '## Uninstall' README.md && echo README_OK</automated>
  </verify>
  <acceptance_criteria>
    - `test -f README.md` exits 0
    - `wc -l < README.md` reports between 40 and 80 lines (inclusive)
    - `grep -q '# LibreSpin' README.md` exits 0 (h1 title)
    - `grep -q '## Install' README.md` exits 0
    - `grep -q '## Prerequisites' README.md` exits 0
    - `grep -q '## Quick Start' README.md` exits 0
    - `grep -q '## Uninstall' README.md` exits 0
    - `grep -q '/plugin marketplace add LibreSpin/LibreSpin' README.md` exits 0
    - `grep -q '/plugin install librespin' README.md` exits 0
    - `grep -q 'npx librespin-install' README.md` exits 0
    - `grep -q 'npx librespin-install --uninstall' README.md` exits 0
    - `grep -q '/librespin:concept' README.md` exits 0
    - `grep -q 'Node.js >= 18' README.md` exits 0
    - `grep -ic 'troubleshoot' README.md` reports 0 (D-10 — no troubleshooting section)
    - `grep -ic 'configuration option' README.md` reports 0 (D-10 — no config options)
  </acceptance_criteria>
  <done>README.md replaced; contains marketplace install (primary), npx install (secondary), prerequisites, /librespin:concept first-run walkthrough, 9-phase what-to-expect, uninstall command; 40-80 lines; no v2 content.</done>
</task>

</tasks>

<verification>
- README.md at repo root, 40-80 lines.
- Contains both install paths, prerequisites, quick-start command, 9-phase summary, uninstall command.
- Does NOT contain troubleshooting or configuration sections (D-10).
</verification>

<success_criteria>
- PKG-03 satisfied: README has install, first-run walkthrough, what-to-expect.
- A stranger arriving from the plugin marketplace can read the README top-to-bottom in under 2 minutes and successfully run `/librespin:concept`.
</success_criteria>

<output>
After completion, create `.planning/phases/04-distribution-and-docs/04-03-SUMMARY.md`
</output>
