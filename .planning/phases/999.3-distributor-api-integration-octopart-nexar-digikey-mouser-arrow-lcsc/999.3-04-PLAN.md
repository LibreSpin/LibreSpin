---
phase: 999.3-distributor-api-integration-octopart-nexar-digikey-mouser-arrow-lcsc
plan: 04
type: execute
id: 999.3-04
wave: 1
depends_on: []
files_modified:
  - skills/setup/SKILL.md
requirements:
  - DIST-02
autonomous: true
gap_closure: true
must_haves:
  truths:
    - "Running /librespin:setup with parts_used=85 shows an 80-part warning before the LM358 validation call"
    - "Running /librespin:setup with parts_used=100 skips the LM358 validation call and shows an exhaustion message"
    - "After a successful LM358 validation test, parts_used in credentials is n+1, not 0"
  artifacts:
    - path: "skills/setup/SKILL.md"
      provides: "Nexar quota pre-check and accurate increment logic"
      contains: "parts_used >= 80"
  key_links:
    - from: "skills/setup/SKILL.md (Nexar section)"
      to: "~/.librespin/credentials parts_used"
      via: "read_credential nexar parts_used before test; write_credential nexar parts_used $((CURRENT_PARTS + 1)) after"
      pattern: "CURRENT_PARTS.*read_credential nexar parts_used"
---

# Plan 999.3-04: Fix DIST-02 — Nexar quota tracking in setup skill

## Objective

Two bugs exist in `skills/setup/SKILL.md`'s Nexar section (Step 4.1) that cause inaccurate quota tracking:

1. **Reset bug (line 265):** `write_credential nexar parts_used 0` resets the lifetime counter to 0 on every successful setup re-run. A user who has consumed 85 parts and re-runs setup ends up showing 0 — the counter is erased, not maintained.

2. **Missing pre-check:** The LM358 validation test (line 246) fires unconditionally. Users near or at the 100-part free-tier limit receive no warning and may unknowingly exhaust their quota during setup re-runs.

This plan makes two surgical edits to close the DIST-02 gap. No other files are touched.

## Tasks

<task id="1">
<title>Add pre-quota-check before Nexar LM358 validation</title>
<read_first>
- skills/setup/SKILL.md — read lines 238–260 to see the full `if [ -z "$NEXAR_TOKEN" ]` block. The LM358 test query starts at line 246 (inside the `else` branch, after `echo "TOKEN_OK"`). Insert the quota pre-check between line 244 (NEXAR_EXPIRY assignment) and line 246 (the NEXAR_TEST curl call).
</read_first>
<action>
Locate the block that begins at line 241 (`else`) and contains the comment `# Test query — validate token works and API is reachable` (line 245). Insert the following quota pre-check block immediately BEFORE that comment line (between the NEXAR_EXPIRY line and the "# Test query" comment), replacing the existing lines 245–258 content as shown:

OLD (lines 245–258 — the test query block inside the `else` branch):
```
  # Test query — validate token works and API is reachable
  NEXAR_TEST=$(curl -s -X POST 'https://api.nexar.com/graphql/' \
    -H "Authorization: Bearer $NEXAR_TOKEN" \
    -H 'Content-Type: application/json' \
    -d '{"query": "query { supSearchMpn(q: \"LM358\", limit: 1) { hits } }"}')
  NEXAR_HITS=$(echo "$NEXAR_TEST" | jq -r '.data.supSearchMpn.hits // 0')
  NEXAR_TEST_ERROR=$(echo "$NEXAR_TEST" | jq -r '.errors[0].message // empty')

  if [ "$NEXAR_HITS" -gt 0 ] 2>/dev/null; then
    echo "VALID: hits=$NEXAR_HITS"
  else
    echo "INVALID: ${NEXAR_TEST_ERROR:-no hits returned for LM358 test query}"
  fi
```

NEW (replacement — inserts quota pre-check then runs test only when quota allows):
```
  # Quota pre-check — read current parts_used before consuming one more
  CURRENT_PARTS=$(read_credential nexar parts_used 2>/dev/null || echo 0)
  CURRENT_PARTS=$(( CURRENT_PARTS + 0 ))  # ensure numeric

  if [ "$CURRENT_PARTS" -ge 100 ]; then
    echo "QUOTA_EXHAUSTED: Nexar quota exhausted (${CURRENT_PARTS}/100 parts used). Cannot run validation test — credentials saved without live test."
  else
    if [ "$CURRENT_PARTS" -ge 80 ]; then
      echo "QUOTA_WARNING: You have used ${CURRENT_PARTS}/100 Nexar free-tier parts. Approaching quota limit."
    fi

    # Test query — validate token works and API is reachable
    NEXAR_TEST=$(curl -s -X POST 'https://api.nexar.com/graphql/' \
      -H "Authorization: Bearer $NEXAR_TOKEN" \
      -H 'Content-Type: application/json' \
      -d '{"query": "query { supSearchMpn(q: \"LM358\", limit: 1) { hits } }"}')
    NEXAR_HITS=$(echo "$NEXAR_TEST" | jq -r '.data.supSearchMpn.hits // 0')
    NEXAR_TEST_ERROR=$(echo "$NEXAR_TEST" | jq -r '.errors[0].message // empty')

    if [ "$NEXAR_HITS" -gt 0 ] 2>/dev/null; then
      echo "VALID: hits=$NEXAR_HITS"
    else
      echo "INVALID: ${NEXAR_TEST_ERROR:-no hits returned for LM358 test query}"
    fi
  fi
```

Also update the heading comment on line 261 from:
```
**On VALID:** Write token and expiry, reset parts_used:
```
to:
```
**On VALID (and on QUOTA_EXHAUSTED):** Write token and expiry; increment parts_used by 1 if test ran:
```
</action>
<acceptance_criteria>
- `grep -n "CURRENT_PARTS.*-ge 80" skills/setup/SKILL.md` returns at least 1 match
- `grep -n "CURRENT_PARTS.*-ge 100" skills/setup/SKILL.md` returns at least 1 match
- `grep -n "QUOTA_WARNING\|Approaching quota" skills/setup/SKILL.md` returns at least 1 match
- `grep -n "QUOTA_EXHAUSTED\|quota exhausted" skills/setup/SKILL.md` returns at least 1 match
- `grep -n "CURRENT_PARTS=\$(read_credential nexar parts_used" skills/setup/SKILL.md` returns at least 1 match
</acceptance_criteria>
</task>

<task id="2">
<title>Fix parts_used reset — increment instead of zero</title>
<read_first>
- skills/setup/SKILL.md — read lines 261–268. Line 265 currently reads `write_credential nexar parts_used 0`. This is the line to replace. The `CURRENT_PARTS` variable is available from Task 1's pre-check block (same bash scope, inside the same `else` branch of the token check).
</read_first>
<action>
Locate line 265 (inside the "On VALID" bash block):

OLD (lines 262–266, the full bash code block after the "On VALID" heading):
```bash
write_credential nexar access_token "$NEXAR_TOKEN"
write_credential nexar token_expires "$NEXAR_EXPIRY"
write_credential nexar parts_used 0
```

NEW — replace the three-line block above with the following five-line block (increment by 1 if the test ran; preserve existing count if quota was exhausted):
```bash
write_credential nexar access_token "$NEXAR_TOKEN"
write_credential nexar token_expires "$NEXAR_EXPIRY"
if [ "$CURRENT_PARTS" -lt 100 ]; then
  write_credential nexar parts_used $(( CURRENT_PARTS + 1 ))
else
  # Quota exhausted — preserve the existing count, do not increment past 100
  write_credential nexar parts_used "$CURRENT_PARTS"
fi
```

The surrounding context (the `**On VALID (and on QUOTA_EXHAUSTED):**` heading updated in Task 1) stays as written by Task 1.
</action>
<acceptance_criteria>
- `grep -n "write_credential nexar parts_used 0" skills/setup/SKILL.md` returns NO matches
- `grep -n "CURRENT_PARTS + 1" skills/setup/SKILL.md` returns at least 1 match
- `grep -n "write_credential nexar parts_used" skills/setup/SKILL.md` returns at least 1 match (the new increment line)
</acceptance_criteria>
</task>

## Verification Criteria

- DIST-02 quota tracking fully satisfied after both tasks:
  - `grep "CURRENT_PARTS.*-ge 80" skills/setup/SKILL.md` — warning threshold present
  - `grep "CURRENT_PARTS.*-ge 100" skills/setup/SKILL.md` — exhaustion skip present
  - `grep "CURRENT_PARTS + 1" skills/setup/SKILL.md` — increment-not-reset logic present
  - `grep "write_credential nexar parts_used 0" skills/setup/SKILL.md` — returns NO matches (reset bug gone)

## must_haves

- The Nexar quota counter in `~/.librespin/credentials` is accurate after re-running setup (increments by 1 per test, never resets to 0)
- Users with parts_used >= 80 see a "Approaching quota limit" warning when running setup
- Users with parts_used >= 100 get a clear "quota exhausted" message; setup saves the token without making an additional LM358 API call

## Output

After completion, create `.planning/phases/999.3-distributor-api-integration-octopart-nexar-digikey-mouser-arrow-lcsc/999.3-04-SUMMARY.md`
