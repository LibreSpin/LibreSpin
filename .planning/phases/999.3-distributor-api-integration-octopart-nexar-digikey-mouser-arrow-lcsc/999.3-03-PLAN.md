---
phase: 999.3-distributor-api-integration-octopart-nexar-digikey-mouser-arrow-lcsc
plan: 03
type: execute
wave: 2
depends_on:
  - 999.3-01
files_modified:
  - bin/install.js
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements:
  - DIST-12

must_haves:
  truths:
    - "Running npx librespin-install copies skills/setup/SKILL.md to ~/.claude/skills/setup/"
    - "Running npx librespin-install --uninstall removes skills/setup/ cleanly"
    - "REQUIREMENTS.md lists DIST-01 through DIST-12 as the formal requirement IDs for phase 999.3"
    - "ROADMAP.md phase 999.3 entry has Plans count and plan list updated (3 plans)"
  artifacts:
    - path: "bin/install.js"
      provides: "Updated installer with setup skill copy and uninstall"
      contains: "skills/setup"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Formal requirements for phase 999.3 (DIST-01 through DIST-12)"
      contains: "DIST-01"
    - path: ".planning/ROADMAP.md"
      provides: "Updated phase 999.3 plan list and plan count"
      contains: "999.3-01-PLAN.md"
  key_links:
    - from: "bin/install.js install()"
      to: "skills/setup/"
      via: "cp recursive"
      pattern: "skills.*setup"
    - from: "bin/install.js INSTALL_ITEMS"
      to: "skills/setup"
      via: "array entry for uninstall"
      pattern: "setup.*recursive: true"
---

<objective>
Update bin/install.js to distribute the new /librespin:setup skill alongside existing skills. Also add formal DIST-xx requirement IDs to REQUIREMENTS.md and update ROADMAP.md to reflect the 3 plans created.

Purpose: Completes the distribution chain so setup skill reaches users via npx librespin-install and plugin install. Formal requirements make 999.3 traceable.

Output: Updated bin/install.js, .planning/REQUIREMENTS.md, .planning/ROADMAP.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/999.3-distributor-api-integration-octopart-nexar-digikey-mouser-arrow-lcsc/999.3-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update bin/install.js to include setup skill</name>
  <files>bin/install.js</files>
  <read_first>
    - bin/install.js — full file to understand install() function structure and INSTALL_ITEMS array pattern
  </read_first>
  <action>
Make two changes to bin/install.js:

**Change 1: Add setup skill directory creation** in the `dirs` array inside `install()`:
```javascript
// Current dirs array:
const dirs = [
  'skills/concept',
  'skills/calcpad',
  'skills/simulate',
  'agents',
  'librespin/templates',
];

// Updated dirs array (add 'skills/setup'):
const dirs = [
  'skills/concept',
  'skills/calcpad',
  'skills/simulate',
  'skills/setup',
  'agents',
  'librespin/templates',
];
```

**Change 2: Add cp call for setup skill** in install() after the existing simulate cp block (after line ~74):
```javascript
    await cp(
      join(sourceBase, 'skills', 'setup'),
      join(targetBase, 'skills', 'setup'),
      { recursive: true, force: true }
    );
```

Update the console.log line immediately after the three skill cp blocks to include 'setup':
```javascript
    console.log('  ✓ Skills installed (concept, calcpad, simulate, setup)');
```

**Change 3: Add setup skill to INSTALL_ITEMS array** for uninstall support. Add after the simulate entry:
```javascript
  { path: join('skills', 'setup'), recursive: true },
```

**Change 4: Update restart message** at the end of install() to mention /librespin:setup:
```javascript
      console.log('\nRestart Claude Code to activate /librespin:concept, /librespin:calcpad, /librespin:simulate, and /librespin:setup skills.');
```
  </action>
  <verify>
    <automated>node -e "import('/home/william/repo/LibreSpin/LibreSpin/bin/install.js').catch(e => { if(e.message.includes('Cannot find module') || e.message.includes('ENOENT')) { process.exit(0) } else { process.exit(1) } })" || grep -c "skills/setup" /home/william/repo/LibreSpin/LibreSpin/bin/install.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "skills.*setup" bin/install.js` returns 4 or more (dirs array entry + cp call + INSTALL_ITEMS entry + log message)
    - `grep "recursive: true, force: true" bin/install.js | wc -l` returns 4 (concept, calcpad, simulate, setup)
    - `grep "skills/setup" bin/install.js | wc -l` returns 3 or more (mkdir, cp, INSTALL_ITEMS)
    - `grep "setup" bin/install.js | grep "INSTALL_ITEMS\|recursive: true" | wc -l` returns 1 (INSTALL_ITEMS entry for uninstall)
    - `node --input-type=module < bin/install.js --help` exits 0 (file parses without syntax error) — OR alternatively verify with: `node -e "const {readFileSync} = require('fs'); readFileSync('bin/install.js', 'utf8'); console.log('ok')"` returns ok
  </acceptance_criteria>
  <done>bin/install.js installs and uninstalls skills/setup/ alongside existing skills. Console output messages updated to include 'setup'.</done>
</task>

<task type="auto">
  <name>Task 2: Add DIST requirements to REQUIREMENTS.md and update ROADMAP.md</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md — full file — find the ## Future Requirements or ## v0.2 section to append the new distributor requirements block after
    - .planning/ROADMAP.md — Phase 999.3 section (lines ~103-119) — update plan count and plan list
  </read_first>
  <action>
**REQUIREMENTS.md changes:**

Add a new `## v0.3 Requirements` section after the existing `## v0.2 Requirements` section (before `## Future Requirements`). Insert the following block:

```markdown
## v0.3 Requirements

Requirements for Milestone 3 (v0.3 — Distributor API Integration). Phase 999.3.

### Distributor API Integration

- [ ] **DIST-01**: /librespin:setup skill walks user through credential entry for Nexar, DigiKey, Mouser, Arrow, Newark/Farnell, and LCSC — each supplier is optional and can be skipped
- [ ] **DIST-02**: Nexar OAuth 2.0 client credentials implemented via curl; free tier quota (100 parts) tracked in credentials file; user warned at 80 parts and blocked at 100 with clear message
- [ ] **DIST-03**: DigiKey OAuth 2.0 client credentials implemented via curl; token auto-refreshed before each API call (10-minute lifetime); X-DIGIKEY-Client-Id header included on all requests
- [ ] **DIST-04**: Mouser API key auth implemented; part_api_key stored in [mouser] credentials section; POST endpoint used for part search
- [ ] **DIST-05**: Arrow API key auth implemented; both login (email) and api_key stored in [arrow] section; both required as URI params
- [ ] **DIST-06**: Newark/Farnell element14 API key auth implemented; storefront selectable (us.newark.com default); LCSC supports official API key or public wmsc endpoint fallback
- [ ] **DIST-07**: Credentials stored in INI format at ~/.librespin/credentials; OAuth tokens cached with expiry timestamps; setup validates each API with LM358 test call before saving
- [ ] **DIST-08**: Concept skill Phase 4 enrichment fires automatically after MPN selection when credentials file is present; queries all configured suppliers per MPN
- [ ] **DIST-09**: Phase 4 falls back to existing WebFetch behavior when ~/.librespin/credentials is absent or all sections empty — no errors raised (D-13)
- [ ] **DIST-10**: Any supplier API failure logs inline and continues; workflow never blocked by API errors (D-17)
- [ ] **DIST-11**: Enrichment results appended to .librespin/04-bom/ BOM entries; downstream output contracts (07-final-output/, 08-calculations/) unchanged (D-14)
- [ ] **DIST-12**: bin/install.js copies skills/setup/ alongside existing skills; npx librespin-install --uninstall removes skills/setup/ cleanly
```

Also add traceability entries to the existing `## Traceability` table at the bottom of REQUIREMENTS.md:

```markdown
| DIST-01 | Phase 999.3 | Pending |
| DIST-02 | Phase 999.3 | Pending |
| DIST-03 | Phase 999.3 | Pending |
| DIST-04 | Phase 999.3 | Pending |
| DIST-05 | Phase 999.3 | Pending |
| DIST-06 | Phase 999.3 | Pending |
| DIST-07 | Phase 999.3 | Pending |
| DIST-08 | Phase 999.3 | Pending |
| DIST-09 | Phase 999.3 | Pending |
| DIST-10 | Phase 999.3 | Pending |
| DIST-11 | Phase 999.3 | Pending |
| DIST-12 | Phase 999.3 | Pending |
```

Update the Coverage comment at the bottom to add: "v0.3 requirements: 12 total (Phase 999.3)"

**ROADMAP.md changes:**

Find the Phase 999.3 entry in the Backlog section (around line 103). Replace:
```markdown
**Plans:** 0 plans (promote with /gsd:review-backlog when ready)
```
with:
```markdown
**Plans:** 3 plans
Plans:
- [ ] 999.3-01-PLAN.md — /librespin:setup skill (credential setup for 6 distributor APIs)
- [ ] 999.3-02-PLAN.md — Concept Phase 4 enrichment block (live inventory + pricing via distributor APIs)
- [ ] 999.3-03-PLAN.md — Installer update + formal DIST requirements
```

Also update the Progress table at the bottom of ROADMAP.md to add a row for Phase 999.3:
```markdown
| 999.3. Distributor API Integration | v0.3 | 0/3 | Planned | |
```
  </action>
  <verify>
    <automated>grep -c "DIST-01" /home/william/repo/LibreSpin/LibreSpin/.planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep "DIST-01" .planning/REQUIREMENTS.md` returns match
    - `grep "DIST-12" .planning/REQUIREMENTS.md` returns match
    - `grep "v0.3 Requirements" .planning/REQUIREMENTS.md` returns match
    - `grep "999.3-01-PLAN.md" .planning/ROADMAP.md` returns match
    - `grep "999.3-02-PLAN.md" .planning/ROADMAP.md` returns match
    - `grep "999.3-03-PLAN.md" .planning/ROADMAP.md` returns match
    - `grep "3 plans" .planning/ROADMAP.md` returns match (in phase 999.3 entry)
    - `grep -c "DIST-" .planning/REQUIREMENTS.md` returns 24 or more (12 requirements + 12 traceability rows)
  </acceptance_criteria>
  <done>REQUIREMENTS.md has DIST-01 through DIST-12 in a v0.3 Requirements section with traceability entries. ROADMAP.md phase 999.3 shows 3 plans with their names and objectives.</done>
</task>

</tasks>

<verification>
- `grep -c "skills.*setup" bin/install.js` returns 3 or more
- `grep "DIST-01" .planning/REQUIREMENTS.md` returns match
- `grep "DIST-12" .planning/REQUIREMENTS.md` returns match
- `grep "999.3-01-PLAN.md" .planning/ROADMAP.md` returns match
</verification>

<success_criteria>
bin/install.js distributes skills/setup/ alongside concept/calcpad/simulate, including uninstall support. REQUIREMENTS.md has 12 formal DIST requirements for phase 999.3. ROADMAP.md phase 999.3 entry lists all 3 plans with descriptions.
</success_criteria>

<output>
After completion, create `.planning/phases/999.3-distributor-api-integration-octopart-nexar-digikey-mouser-arrow-lcsc/999.3-03-SUMMARY.md`
</output>
