---
phase: 03-end-to-end-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/skills/librespin-concept/SKILL.md
autonomous: true
requirements:
  - CW-09

must_haves:
  truths:
    - "The 'REQUIREMENTS GATHERING ONLY' guard at line 7023 is removed"
    - "Phase dispatch logic exists so the agent routes to the correct phase based on state.md"
    - "Config.yaml creation is confirmed present in Phase 1 output section"
    - "Phase state value strings are documented for all 7 phases"
  artifacts:
    - path: ".claude/skills/librespin-concept/SKILL.md"
      provides: "Phase dispatch logic replacing the guard, config.yaml creation if missing"
      contains: "PHASE DISPATCH"
  key_links:
    - from: ".librespin/state.md"
      to: "SKILL.md phase dispatch"
      via: "state.phase value matching"
      pattern: "phase.*requirements-gathering|architecture-drafting|validation-gate|component-research|concept-generation|self-critique|final-output"
---

<objective>
Remove the "REQUIREMENTS GATHERING ONLY" execution guard from SKILL.md and replace it with a proper phase dispatch block that routes to the correct workflow phase based on the state.md phase value. Also verify and fix config.yaml creation in Phase 1 output.

Purpose: Without phase dispatch, Phases 2-7 cannot execute even though they are fully designed in SKILL.md. This is the prerequisite for all CW-03 through CW-08 validation.
Output: Updated SKILL.md with working multi-phase dispatch logic.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-end-to-end-validation/03-CONTEXT.md
@.planning/phases/03-end-to-end-validation/03-RESEARCH.md

<interfaces>
<!-- Key state values and phase structure from SKILL.md -->

Phase 1 writes to state.md (line 1065-1072):
```yaml
phase: '3-requirements-gathering'
completed: <ISO date>
completeness_score: <number>
source: 'interactive' | 'yaml'
schema_version: 1
```

Orchestrator Step 3 (line 59-75):
- Reads `.librespin/state.md`
- Parses current phase from frontmatter
- Passes PHASE parameter to agent spawn

Orchestrator Step 4 (line 76-102):
- Spawns agent with `PHASE: requirements` parameter
- Agent receives: config.yaml, state.md, INPUT_FILE

Phase headers in SKILL.md:
- Line 272: PHASE 1: REQUIREMENTS GATHERING
- Line 1114: PHASE 2: ARCHITECTURE DRAFTING
- Line 2014: PHASE 3: VALIDATION GATE
- Line 3041: PHASE 4: COMPONENT RESEARCH
- Line 4498: PHASE 5: CONCEPT GENERATION
- Line 5122: PHASE 6: SELF-CRITIQUE & REFINEMENT
- Line 6310: PHASE 7: FINAL OUTPUT

Guard to remove (line 7023-7028):
```
## CURRENT PHASE: REQUIREMENTS GATHERING ONLY
For Phase 1 (Foundation), only Requirements Gathering is implemented.
Phases 2-7 are designed but not yet executable.
When invoked, execute Phase 1 logic, write requirements.yaml, report completion.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add phase dispatch logic and remove guard</name>
  <files>.claude/skills/librespin-concept/SKILL.md</files>
  <read_first>
    - .claude/skills/librespin-concept/SKILL.md (full file — the system under test)
    - .planning/phases/03-end-to-end-validation/03-RESEARCH.md (phase mapping table, state values, pitfalls)
  </read_first>
  <action>
Make three targeted edits to SKILL.md:

**Edit 1: Remove the "REQUIREMENTS GATHERING ONLY" guard (lines 7023-7028)**

Delete the entire section:
```
## CURRENT PHASE: REQUIREMENTS GATHERING ONLY
For Phase 1 (Foundation), only Requirements Gathering is implemented.
Phases 2-7 are designed but not yet executable.
When invoked, execute Phase 1 logic, write requirements.yaml, report completion.
```

**Edit 2: Add phase dispatch section**

Insert a new `## PHASE DISPATCH` section immediately BEFORE the `## CONSTRAINTS` section (which currently follows the removed guard). This section tells the agent which phase to execute based on state:

```markdown
## PHASE DISPATCH

On each invocation, determine which phase to execute by reading `.librespin/state.md` frontmatter.

**Dispatch rules:**

| state.md `phase` value | Meaning | Execute Next |
|------------------------|---------|--------------|
| (no state file exists) | Fresh project | Phase 1: Requirements Gathering |
| `3-requirements-gathering` | Phase 1 complete | Phase 2: Architecture Drafting |
| `2-architecture-drafting` | Phase 2 complete | Phase 2.5 + Phase 3: Req-to-Component Mapping then Validation Gate |
| `3-validation-gate` | Phase 3 complete | Phase 4: Component Research |
| `4-component-research` | Phase 4 complete | Phase 5: Concept Generation |
| `5-concept-generation` | Phase 5 complete | Phase 6: Self-Critique & Refinement |
| `6-self-critique` | Phase 6 complete | Phase 7: Final Output |
| `7-final-output` | All phases complete | Report completion, no further phases |

**Implementation:**

```
Read .librespin/state.md frontmatter
Extract `phase` field value

if no state file OR phase is empty:
  Execute PHASE 1: REQUIREMENTS GATHERING
elif phase == "3-requirements-gathering":
  Execute PHASE 2: ARCHITECTURE DRAFTING
elif phase == "2-architecture-drafting":
  Execute PHASE 2.5: REQUIREMENTS-TO-COMPONENT MAPPING
  then Execute PHASE 3: VALIDATION GATE
elif phase == "3-validation-gate":
  Execute PHASE 4: COMPONENT RESEARCH
elif phase == "4-component-research":
  Execute PHASE 5: CONCEPT GENERATION
elif phase == "5-concept-generation":
  Execute PHASE 6: SELF-CRITIQUE & REFINEMENT
elif phase == "6-self-critique":
  Execute PHASE 7: FINAL OUTPUT
elif phase == "7-final-output":
  Report: "All phases complete. Output at .librespin/07-final-output/"
else:
  Error: "Unknown phase value: {phase}. Check .librespin/state.md"
```

**State update contract:** Each phase MUST update `.librespin/state.md` frontmatter `phase` field to its own phase value from the table above upon completion. This enables the next invocation to dispatch correctly.
```

**Edit 3: Add state update instructions to each phase's OUTPUT section**

For each of Phases 2 through 7, find the OUTPUT section and ensure it includes a state.md update that writes the correct `phase` value. Specifically, add or verify these state writes:

- Phase 2 OUTPUT (around line 1114+): write `phase: '2-architecture-drafting'` to state.md frontmatter
- Phase 2.5/3 OUTPUT: write `phase: '3-validation-gate'` to state.md frontmatter
- Phase 4 OUTPUT: write `phase: '4-component-research'` to state.md frontmatter
- Phase 5 OUTPUT: write `phase: '5-concept-generation'` to state.md frontmatter
- Phase 6 OUTPUT: write `phase: '6-self-critique'` to state.md frontmatter
- Phase 7 OUTPUT: write `phase: '7-final-output'` to state.md frontmatter

If a phase already has a state update block, verify the phase value string matches the dispatch table. If it does not have one, add a state update block following the same pattern as Phase 1's output (lines 1060-1089):

```javascript
// Update state.md frontmatter
const stateUpdate = `---
phase: '{phase-value}'
completed: ${new Date().toISOString()}
---
`;
// Append phase-specific tracking below frontmatter
```

**Edit 4: Verify config.yaml creation in Phase 1**

Search the Phase 1 OUTPUT section (lines 1040-1110) for any config.yaml write. Per the research (Pitfall 3), config.yaml creation may be missing. If it is NOT present, add config.yaml creation between the requirements.yaml write and the state.md write:

```javascript
// Create config.yaml with defaults (if not exists)
const configPath = '.librespin/config.yaml';
if (!fs.existsSync(configPath)) {
  const defaultConfig = yaml.dump({
    draft_count: 5,
    iteration_limit: 5,
    confidence_threshold: 80
  });
  fs.writeFileSync(configPath, defaultConfig);
}
```

Also update the Phase 1 output summary display to mention config.yaml:
```
Config saved to: .librespin/config.yaml (draft_count: 5, iteration_limit: 5, confidence_threshold: 80)
```

**Edit 5: Update orchestrator PHASE parameter**

In Step 4 (line 76-102), the orchestrator currently hardcodes `PHASE: requirements`. Update it to pass the actual phase determined from state:

Change:
```
- PHASE: requirements
```

To:
```
- PHASE: (determined from state — see PHASE DISPATCH section)
```

Or remove the PHASE parameter entirely since the agent reads state.md directly and uses the dispatch table.

Per D-05: only fix what blocks workflow completion. Per D-06: do not improve prompts or formatting.
  </action>
  <verify>
    <automated>cd /home/william/repo/LibreSpin/LibreSpin && grep -c "CURRENT PHASE: REQUIREMENTS GATHERING ONLY" .claude/skills/librespin-concept/SKILL.md | grep "^0$" && grep -c "PHASE DISPATCH" .claude/skills/librespin-concept/SKILL.md | grep -v "^0$" && grep "config.yaml" .claude/skills/librespin-concept/SKILL.md | grep -c "draft_count" | grep -v "^0$" && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - SKILL.md does NOT contain the string "CURRENT PHASE: REQUIREMENTS GATHERING ONLY"
    - SKILL.md contains a "## PHASE DISPATCH" section with the dispatch table
    - SKILL.md Phase 1 OUTPUT section contains config.yaml creation with `draft_count: 5, iteration_limit: 5, confidence_threshold: 80`
    - Each of Phases 2-7 OUTPUT sections contain a state.md update with the correct phase value string
    - The dispatch table maps all 7 phase values: 3-requirements-gathering, 2-architecture-drafting, 3-validation-gate, 4-component-research, 5-concept-generation, 6-self-critique, 7-final-output
    - Orchestrator Step 4 no longer hardcodes `PHASE: requirements`
  </acceptance_criteria>
  <done>SKILL.md has working phase dispatch logic, the guard is removed, config.yaml creation is present in Phase 1, and all phase state transitions are documented and wired.</done>
</task>

</tasks>

<verification>
- `grep "CURRENT PHASE: REQUIREMENTS GATHERING ONLY" .claude/skills/librespin-concept/SKILL.md` returns no matches
- `grep "## PHASE DISPATCH" .claude/skills/librespin-concept/SKILL.md` returns a match
- `grep -A2 "phase.*dispatch" .claude/skills/librespin-concept/SKILL.md` shows the dispatch section exists
- `grep "config.yaml" .claude/skills/librespin-concept/SKILL.md` includes config creation in Phase 1 output
- Each phase value string appears in both the dispatch table and the corresponding phase OUTPUT section
</verification>

<success_criteria>
SKILL.md is ready for multi-phase execution: the guard is gone, dispatch logic routes based on state.md, config.yaml is created in Phase 1, and all 7 phases write their state correctly.
</success_criteria>

<output>
After completion, create `.planning/phases/03-end-to-end-validation/03-01-SUMMARY.md`
</output>
