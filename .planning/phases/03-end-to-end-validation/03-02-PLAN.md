---
phase: 03-end-to-end-validation
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - .librespin/test-fixtures/requirements-complete.yaml
  - .librespin/test-fixtures/requirements-stripped.yaml
  - .planning/phases/03-end-to-end-validation/03-VALIDATION-CHECKLIST.md
autonomous: false
requirements:
  - CW-01
  - CW-02
  - CW-09
  - CW-10

must_haves:
  truths:
    - "User can start interactive requirements gathering via AskUserQuestion and reach Phase 1 completion"
    - "User can provide a YAML file and the agent ingests it without error"
    - "Completeness scoring accepts complete requirements (score >= 70)"
    - "Completeness scoring rejects stripped requirements (score < 70, triggers gap-fill)"
    - "Workflow resumes from last completed phase after interruption"
  artifacts:
    - path: ".librespin/test-fixtures/requirements-complete.yaml"
      provides: "Complete test fixture for YAML import happy path"
      contains: "schema_version: 1"
    - path: ".librespin/test-fixtures/requirements-stripped.yaml"
      provides: "Stripped test fixture for gap-fill trigger path"
      contains: "schema_version: 1"
    - path: ".planning/phases/03-end-to-end-validation/03-VALIDATION-CHECKLIST.md"
      provides: "Evidence checklist mapping CW-01 through CW-10 to test results"
      contains: "CW-01"
  key_links:
    - from: "requirements-complete.yaml"
      to: "/librespin:concept --input"
      via: "YAML import mode"
      pattern: "schema_version.*1"
    - from: ".librespin/state.md"
      to: "resume logic"
      via: "orchestrator reads phase from state frontmatter"
      pattern: "phase.*requirements-gathering"
---

<objective>
Create test fixtures and the validation evidence checklist, then validate Phase 1 (requirements gathering) for both interactive and YAML import modes, plus completeness scoring and resume behavior.

Purpose: CW-01 (interactive input), CW-02 (YAML import), CW-09 (resume), and CW-10 (scoring) are testable with Phase 1 alone. Validating these first confirms the foundation before testing Phases 2-7.
Output: Test fixtures, validation checklist with Phase 1 evidence captured, any blocking bug fixes applied.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-end-to-end-validation/03-CONTEXT.md
@.planning/phases/03-end-to-end-validation/03-RESEARCH.md
@.planning/phases/03-end-to-end-validation/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test fixtures and validation checklist</name>
  <files>
    .librespin/test-fixtures/requirements-complete.yaml
    .librespin/test-fixtures/requirements-stripped.yaml
    .planning/phases/03-end-to-end-validation/03-VALIDATION-CHECKLIST.md
  </files>
  <read_first>
    - .claude/librespin/templates/requirements.yaml (the template with IoT sensor node example — use as basis for complete fixture)
    - .planning/REQUIREMENTS.md (CW-01 through CW-10 definitions for checklist)
    - .planning/phases/03-end-to-end-validation/03-RESEARCH.md (stripped YAML example in Code Examples section, scoring formula, critical/important/nice-to-have field lists)
    - .claude/skills/librespin-concept/SKILL.md (completeness scoring section for field lists — search for "calculateCompletenessScore")
  </read_first>
  <action>
**File 1: `.librespin/test-fixtures/requirements-complete.yaml`**

Copy the existing IoT sensor node example from `.claude/librespin/templates/requirements.yaml` verbatim. This is a complete example that should score near 100/100, exercising the happy path for CW-02 and CW-10.

**File 2: `.librespin/test-fixtures/requirements-stripped.yaml`**

Create a stripped-down LED driver requirements file per the research code example. This covers critical fields only, omitting all important (hmi.*, physical.*) and nice-to-have (production.*, compliance, lifecycle, preferences) fields:

```yaml
schema_version: 1
project_name: "LED Driver Test"
use_case: |
  Constant-current LED driver for a 12V LED strip.
environment:
  location: indoor
  temperature_min_c: 15
  temperature_max_c: 40
connectivity:
  primary: none
  region: "N/A"
  port_count: 0
  hub_acceptable: false
power:
  source: wall
  battery_life_target: "N/A"
```

Note: `connectivity.region`, `connectivity.port_count`, and `connectivity.hub_acceptable` are included because they are critical fields per the scoring formula (11 critical fields). Setting them to N/A/0/false tests whether the scoring treats these as "answered" for non-wireless projects (research Open Question 2). Expected score: approximately 50-65/100 (all critical answered = 50/50 critical, 0/30 important, 0/20 nice-to-have = 50/100).

**File 3: `.planning/phases/03-end-to-end-validation/03-VALIDATION-CHECKLIST.md`**

Create a markdown checklist per D-07 and D-08:

```markdown
# Phase 3: Validation Checklist

Evidence capture for CW-01 through CW-10.

## CW-01: Interactive Requirements Gathering
- [ ] Ran `/librespin:concept` without --input flag
- [ ] Agent used AskUserQuestion to ask requirements questions
- [ ] Progressive disclosure: critical -> important -> nice-to-have sections visible
- [ ] Phase 1 completed successfully
- Evidence: `.librespin/state.md` shows `phase: '3-requirements-gathering'`
- Evidence: `.librespin/01-requirements/requirements.yaml` exists with gathered data
- Evidence: `.librespin/config.yaml` exists with defaults
- Blocking bugs found: (none / list)

## CW-02: YAML File Import
- [ ] Ran `/librespin:concept --input .librespin/test-fixtures/requirements-complete.yaml`
- [ ] Agent loaded YAML without error
- [ ] Completeness score displayed (expected: near 100/100)
- [ ] Agent offered review option
- [ ] Phase 1 completed successfully
- Evidence: `.librespin/state.md` shows `source: 'yaml'`
- Evidence: `.librespin/01-requirements/requirements.yaml` matches input
- Blocking bugs found: (none / list)

## CW-03: Architecture Concept Generation (Phase 2)
- [ ] Ran `/librespin:concept` after Phase 1 complete
- [ ] Agent generated 5-6 diverse concepts
- [ ] Concepts written to `.librespin/02-concepts/`
- [ ] Overview file exists at `.librespin/02-concepts/overview.md`
- Evidence: `ls .librespin/02-concepts/` shows concept-*.md files
- Blocking bugs found: (none / list)

## CW-04: Validation Gate (Phase 3)
- [ ] Agent validated concepts against confidence threshold
- [ ] Web research used for validation
- [ ] Concepts scored and filtered
- Evidence: Validation sections added to concept files in `.librespin/02-concepts/`
- Evidence: `.librespin/state.md` updated to `phase: '3-validation-gate'`
- Blocking bugs found: (none / list)

## CW-05: Component Research (Phase 4)
- [ ] Agent researched specific components with real part numbers
- [ ] BOM files generated with pricing and availability
- Evidence: `ls .librespin/04-bom/` shows bom-*.md files
- Evidence: BOM files contain MPN (manufacturer part numbers)
- Blocking bugs found: (none / list)

## CW-06: Block Diagrams (Phase 5)
- [ ] Agent generated detailed block diagrams with MPNs
- [ ] Spec analysis performed against requirements
- Evidence: `ls .librespin/05-detailed-designs/` shows analysis-*.md files
- Evidence: Files contain ASCII block diagrams
- Blocking bugs found: (none / list)

## CW-07: Self-Critique & Refinement (Phases 6-8)
- [ ] Agent performed self-critique iteration(s)
- [ ] Concepts refined based on critique
- [ ] Quality scores tracked across iterations
- Evidence: `ls .librespin/06-refinement/` shows score-*.md and refinement-log.md
- Blocking bugs found: (none / list)

## CW-08: Comparison Matrix (Phase 9/7)
- [ ] Agent produced final comparison matrix
- [ ] Recommended concept identified
- Evidence: `.librespin/07-final-output/` contains comparison matrix
- Evidence: State.md shows `phase: '7-final-output'`
- Blocking bugs found: (none / list)

## CW-09: Resume Across Invocations
- [ ] Completed Phase 1, then ran `/librespin:concept` again
- [ ] Agent detected existing state.md and resumed from Phase 2
- [ ] Did NOT repeat Phase 1 questions
- [ ] (Optional) Interrupted mid-Phase-2 and resumed — agent picked up correctly
- Evidence: State.md phase value progressed without regression
- Blocking bugs found: (none / list)

## CW-10: Completeness Scoring
- [ ] Ran with complete YAML (requirements-complete.yaml): score >= 70, no gap-fill triggered
- [ ] Ran with stripped YAML (requirements-stripped.yaml): score < 70, gap-fill questions asked
- [ ] Scoring breakdown shows critical/important/nice-to-have components
- [ ] After gap-fill, recalculated score met threshold
- Evidence: Score output captured for both test cases
- Blocking bugs found: (none / list)

## Bug Fixes Applied
| Bug | CW Affected | Fix Description | File Changed |
|-----|-------------|-----------------|--------------|
| (list each blocking bug fixed during validation) | | | |

## Context Pressure Observations
- SKILL.md token count: ~58,000 tokens
- Phase where context pressure first observed: (phase number or "none")
- Symptoms: (truncation, skipped steps, abbreviated output, or "none")
- Recommendation for OPT-01 priority: (low / medium / high)
```
  </action>
  <verify>
    <automated>cd /home/william/repo/LibreSpin/LibreSpin && test -f .librespin/test-fixtures/requirements-complete.yaml && test -f .librespin/test-fixtures/requirements-stripped.yaml && test -f .planning/phases/03-end-to-end-validation/03-VALIDATION-CHECKLIST.md && grep "schema_version" .librespin/test-fixtures/requirements-complete.yaml && grep "schema_version" .librespin/test-fixtures/requirements-stripped.yaml && grep "CW-01" .planning/phases/03-end-to-end-validation/03-VALIDATION-CHECKLIST.md && grep "CW-10" .planning/phases/03-end-to-end-validation/03-VALIDATION-CHECKLIST.md && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `.librespin/test-fixtures/requirements-complete.yaml` exists and contains `schema_version: 1` with a complete IoT sensor node example
    - `.librespin/test-fixtures/requirements-stripped.yaml` exists and contains `schema_version: 1` with only critical fields populated (no hmi.*, physical.*, production.*, compliance, lifecycle, preferences)
    - `03-VALIDATION-CHECKLIST.md` contains checkbox entries for all 10 requirements CW-01 through CW-10
    - `03-VALIDATION-CHECKLIST.md` contains an evidence field for each requirement specifying which output files to check
    - `03-VALIDATION-CHECKLIST.md` contains a "Bug Fixes Applied" table and "Context Pressure Observations" section
  </acceptance_criteria>
  <done>Test fixtures and validation checklist are ready for manual walkthrough.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Validate Phase 1 — interactive mode, YAML import, scoring, and resume</name>
  <what-built>Test fixtures and validation checklist created. Phase dispatch logic added in Plan 01. Ready to validate CW-01, CW-02, CW-09, CW-10 via manual walkthrough of Phase 1.</what-built>
  <how-to-verify>
Run these validation scenarios in order. After each, capture evidence in the checklist.

**Scenario 1: YAML import with complete file (CW-02, CW-10 happy path)**
1. Clean slate: `rm -rf .librespin/` (except test-fixtures)
2. Run: `/librespin:concept --input .librespin/test-fixtures/requirements-complete.yaml`
3. Confirm: score >= 70 displayed, no gap-fill triggered, Phase 1 completes
4. Evidence: `cat .librespin/state.md`, `ls .librespin/01-requirements/`, `cat .librespin/config.yaml`

**Scenario 2: YAML import with stripped file (CW-10 gap-fill path)**
1. Clean slate: `rm -rf .librespin/01-requirements .librespin/state.md .librespin/config.yaml`
2. Run: `/librespin:concept --input .librespin/test-fixtures/requirements-stripped.yaml`
3. Confirm: score < 70, gap-fill questions asked for missing important/nice-to-have fields
4. Answer gap-fill questions minimally to reach >= 70
5. Evidence: score output before and after gap-fill

**Scenario 3: Interactive mode (CW-01)**
1. Clean slate: `rm -rf .librespin/01-requirements .librespin/state.md .librespin/config.yaml`
2. Run: `/librespin:concept` (no --input flag)
3. Confirm: AskUserQuestion prompts appear, progressive disclosure (critical first)
4. Answer enough questions to complete Phase 1
5. Evidence: `cat .librespin/state.md`, `cat .librespin/01-requirements/requirements.yaml`

**Scenario 4: Resume (CW-09)**
1. After Scenario 3 completes (state.md exists with `phase: '3-requirements-gathering'`)
2. Run: `/librespin:concept` again
3. Confirm: agent says "Resuming from Phase 2" (not repeating Phase 1)
4. Evidence: state.md phase value, agent's first message

If any scenario fails due to a blocking bug, describe the bug and stop — the executor will fix it per D-05 before continuing.
  </how-to-verify>
  <resume-signal>Type "approved" with evidence notes, or describe blocking bugs found</resume-signal>
</task>

</tasks>

<verification>
- All 4 test scenarios executed
- CW-01, CW-02, CW-09, CW-10 entries in validation checklist updated with evidence
- Any blocking bugs fixed and documented in checklist
</verification>

<success_criteria>
Phase 1 works correctly for both input modes. Completeness scoring correctly accepts complete requirements and rejects stripped requirements. Resume from state.md works (agent advances to Phase 2 instead of repeating Phase 1).
</success_criteria>

<output>
After completion, create `.planning/phases/03-end-to-end-validation/03-02-SUMMARY.md`
</output>
