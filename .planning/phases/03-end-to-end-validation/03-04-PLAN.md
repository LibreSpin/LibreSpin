---
phase: 03-end-to-end-validation
plan: 04
type: execute
wave: 1
depends_on: ["03-03"]
files_modified:
  - .planning/phases/03-end-to-end-validation/03-VALIDATION-CHECKLIST.md
  - .librespin/state.md
  - .librespin/01-requirements/requirements.yaml
  - .librespin/config.yaml
autonomous: false
requirements: [CW-01, CW-02, CW-03, CW-04, CW-05, CW-06, CW-07, CW-08, CW-09, CW-10]
gap_closure: true

must_haves:
  truths:
    - "User started interactive requirements gathering via AskUserQuestion and reached Phase 1 completion"
    - "Completeness scoring rejected stripped requirements (score < 70) and triggered gap-fill AskUserQuestion flow"
    - "After gap-fill, recalculated score was >= 70 allowing progression"
  artifacts:
    - path: ".planning/phases/03-end-to-end-validation/03-VALIDATION-CHECKLIST.md"
      provides: "Updated CW-01 and CW-10 sections with checkboxes checked and live evidence"
      contains: "Status: VALIDATED"
  key_links:
    - from: "AskUserQuestion prompts"
      to: ".librespin/01-requirements/requirements.yaml"
      via: "Interactive answers populate requirements YAML"
      pattern: "source.*interactive"
    - from: "requirements-stripped.yaml"
      to: "AskUserQuestion gap-fill"
      via: "Score < 70 triggers gap-fill questioning"
      pattern: "completeness_score.*[0-6][0-9]"
---

<objective>
Close the two remaining verification gaps for Phase 3: interactive AskUserQuestion requirements gathering (CW-01) and completeness scoring gap-fill path (CW-10 partial).

Purpose: Both gaps require live Claude Code sessions with a human responding to AskUserQuestion prompts. Code changes alone cannot close them. This plan guides the executor through two live test runs, captures evidence, and updates the validation checklist.

Output: Updated 03-VALIDATION-CHECKLIST.md with CW-01 all checkboxes checked and CW-10 gap-fill path confirmed with live evidence.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-end-to-end-validation/03-CONTEXT.md
@.planning/phases/03-end-to-end-validation/03-VERIFICATION.md
@.planning/phases/03-end-to-end-validation/03-VALIDATION-CHECKLIST.md
@.claude/skills/librespin-concept/SKILL.md
@.librespin/test-fixtures/requirements-stripped.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prepare clean test environment for interactive run</name>
  <files>.librespin/state.md, .librespin/config.yaml, .librespin/01-requirements/requirements.yaml</files>
  <read_first>
    - .planning/phases/03-end-to-end-validation/03-VERIFICATION.md
    - .planning/phases/03-end-to-end-validation/03-VALIDATION-CHECKLIST.md
    - .librespin/test-fixtures/requirements-stripped.yaml
  </read_first>
  <action>
    Back up existing .librespin/ output (except test-fixtures/) to .librespin/backup-03-03/ so evidence from the prior end-to-end run is preserved. Then clear the working directories so the interactive test starts fresh:

    1. Create backup directory: `mkdir -p .librespin/backup-03-03`
    2. Move all output directories (01-requirements/, 02-concepts/, 03-validation/, 04-bom/, 05-detailed-designs/, 06-refinement/, 07-final-output/) into backup-03-03/
    3. Move state.md and config.yaml into backup-03-03/
    4. Verify .librespin/test-fixtures/ still exists (do NOT move it)
    5. Verify .librespin/ contains only test-fixtures/ and backup-03-03/

    This gives a clean slate for the interactive requirements gathering test (CW-01).
  </action>
  <verify>
    <automated>ls .librespin/ | sort</automated>
  </verify>
  <acceptance_criteria>
    - `ls .librespin/` shows only `backup-03-03` and `test-fixtures` directories
    - `ls .librespin/backup-03-03/state.md` succeeds (backup preserved)
    - `ls .librespin/test-fixtures/requirements-complete.yaml` succeeds (fixtures untouched)
    - `ls .librespin/test-fixtures/requirements-stripped.yaml` succeeds (fixtures untouched)
    - `test ! -f .librespin/state.md` exits 0 (no state file in working dir)
  </acceptance_criteria>
  <done>Clean .librespin/ directory ready for interactive test. Prior evidence backed up.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live interactive requirements gathering (CW-01)</name>
  <files>.librespin/state.md, .librespin/01-requirements/requirements.yaml, .librespin/config.yaml</files>
  <read_first>
    - .planning/phases/03-end-to-end-validation/03-VERIFICATION.md
    - .claude/skills/librespin-concept/SKILL.md
  </read_first>
  <action>
    Present the user with instructions to run `/librespin:concept` (no --input flag) in a separate Claude Code session. The user must respond to AskUserQuestion prompts interactively using a simple hardware project (e.g., battery-powered temperature sensor). After Phase 1 completes in that session, the user returns to this session to confirm results. Verify .librespin/state.md shows `source: 'interactive'`, .librespin/01-requirements/requirements.yaml exists, and .librespin/config.yaml exists.
  </action>
  <what-built>Clean environment prepared for interactive AskUserQuestion test of CW-01. The user needs to run `/librespin:concept` in a separate Claude Code session (without --input flag) and respond to the interactive requirements questions.</what-built>
  <how-to-verify>
    1. Open a NEW Claude Code session (fresh context, not this one)
    2. Run `/librespin:concept` with NO --input flag
    3. When AskUserQuestion prompts appear, answer them — use a simple project like "battery-powered temperature sensor" or "LED driver circuit"
    4. Answer all critical questions first (use_case, environment, connectivity, power), then important (sensors, hmi, physical), then nice-to-have (production, compliance, lifecycle, preferences)
    5. Let Phase 1 complete (agent writes state.md + requirements.yaml + config.yaml)
    6. After Phase 1 completes, come back to THIS session and confirm:
       - AskUserQuestion prompts appeared (yes/no)
       - Progressive disclosure was observed: critical first, then important, then nice-to-have (yes/no)
       - Phase 1 completed successfully (yes/no)
    7. Then run these checks in this session:
       - `cat .librespin/state.md | head -10` — must show `source: 'interactive'`
       - `ls .librespin/01-requirements/requirements.yaml` — must exist
       - `ls .librespin/config.yaml` — must exist

    If any step fails or the agent errors, describe what happened. That is also valid evidence (a blocking bug to fix per D-05).
  </how-to-verify>
  <verify>
    <automated>cat .librespin/state.md | head -10</automated>
  </verify>
  <done>User confirmed AskUserQuestion prompts appeared, progressive disclosure observed, Phase 1 completed. state.md shows source: interactive.</done>
  <resume-signal>Type "CW-01 passed" with confirmation of the three checks, OR describe the failure</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Capture CW-01 evidence and prepare for CW-10 gap-fill test</name>
  <files>.planning/phases/03-end-to-end-validation/03-VALIDATION-CHECKLIST.md</files>
  <read_first>
    - .planning/phases/03-end-to-end-validation/03-VALIDATION-CHECKLIST.md
    - .librespin/state.md
    - .librespin/01-requirements/requirements.yaml
    - .librespin/config.yaml
  </read_first>
  <action>
    After the user confirms CW-01 passed (or reports a failure):

    IF CW-01 PASSED:
    1. Read .librespin/state.md and confirm `source: 'interactive'` is present
    2. Read .librespin/01-requirements/requirements.yaml and confirm it contains gathered data
    3. Read .librespin/config.yaml and confirm defaults present
    4. Update 03-VALIDATION-CHECKLIST.md CW-01 section:
       - Check all four checkboxes: [x] for each item
       - Update evidence lines with actual values from state.md
       - Change status to: **Status: VALIDATED** (live interactive session confirmed)
    5. Clear .librespin/ working state again (keep backup-03-03/ and test-fixtures/):
       - Move 01-requirements/, state.md, config.yaml to .librespin/backup-cw01/
       - This prepares for the CW-10 stripped YAML test

    IF CW-01 FAILED:
    1. Document the failure in 03-VALIDATION-CHECKLIST.md CW-01 section with exact error
    2. Add to Bug Fixes Applied table
    3. If the bug is fixable (per D-05), fix it and note the fix
    4. Still prepare clean state for CW-10 test

    The CW-10 gap-fill test requires running `/librespin:concept --input .librespin/test-fixtures/requirements-stripped.yaml` in a live session. The stripped YAML has only critical fields filled (project_name, use_case, environment, connectivity, power) and should score approximately 50-60/100, triggering gap-fill via AskUserQuestion.
  </action>
  <verify>
    <automated>grep -c "\[x\]" .planning/phases/03-end-to-end-validation/03-VALIDATION-CHECKLIST.md</automated>
  </verify>
  <acceptance_criteria>
    - CW-01 section in 03-VALIDATION-CHECKLIST.md has either all 4 checkboxes checked OR a documented failure with bug details
    - `grep "VALIDATED" .planning/phases/03-end-to-end-validation/03-VALIDATION-CHECKLIST.md` includes CW-01 line (either "VALIDATED" or "FAILED — see bug fix")
    - .librespin/ is clean for CW-10 test (no state.md in working dir)
    - .librespin/test-fixtures/requirements-stripped.yaml still exists
  </acceptance_criteria>
  <done>CW-01 evidence captured in checklist. Environment clean for CW-10 gap-fill test.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Live completeness scoring gap-fill test (CW-10)</name>
  <files>.librespin/state.md, .librespin/01-requirements/requirements.yaml</files>
  <read_first>
    - .planning/phases/03-end-to-end-validation/03-VERIFICATION.md
    - .librespin/test-fixtures/requirements-stripped.yaml
  </read_first>
  <action>
    Present the user with instructions to run `/librespin:concept --input .librespin/test-fixtures/requirements-stripped.yaml` in a separate Claude Code session. The stripped YAML contains only critical fields and should score below 70, triggering AskUserQuestion gap-fill prompts. The user answers gap-fill questions, and the agent recalculates the score to >= 70. After completion, the user returns to this session to report the initial score, whether gap-fill triggered, the recalculated score, and whether Phase 1 completed.
  </action>
  <what-built>Clean environment prepared for CW-10 gap-fill test. The stripped YAML fixture at `.librespin/test-fixtures/requirements-stripped.yaml` contains only critical fields and should score below 70, triggering AskUserQuestion gap-fill prompts for missing important and nice-to-have fields.</what-built>
  <how-to-verify>
    1. Open a NEW Claude Code session (fresh context)
    2. Run: `/librespin:concept --input .librespin/test-fixtures/requirements-stripped.yaml`
    3. Observe:
       a. Agent should display a completeness score BELOW 70 (expected: approximately 50-60)
       b. Agent should then use AskUserQuestion to ask about missing fields (important: sensors, hmi, physical; nice-to-have: production, compliance, lifecycle, preferences)
       c. Answer the gap-fill questions with reasonable values
       d. Agent should recalculate the score to >= 70 and proceed
    4. After Phase 1 completes (or the agent stops), come back to THIS session and report:
       - Initial completeness score displayed (what number?)
       - Gap-fill AskUserQuestion prompts appeared (yes/no, which fields?)
       - Recalculated score after gap-fill (what number?)
       - Phase 1 completed successfully (yes/no)
    5. Then run these checks in this session:
       - `cat .librespin/state.md | head -15` — check completeness_score value
       - `ls .librespin/01-requirements/requirements.yaml` — must exist with gap-filled data

    If the agent does NOT trigger gap-fill (e.g., scores the stripped YAML >= 70), that is a bug to document. If the agent errors, describe what happened.
  </how-to-verify>
  <verify>
    <automated>cat .librespin/state.md | head -15</automated>
  </verify>
  <done>User confirmed gap-fill triggered with score below 70, AskUserQuestion prompts appeared, recalculated score >= 70, Phase 1 completed.</done>
  <resume-signal>Type "CW-10 passed" with the score values, OR describe the failure</resume-signal>
</task>

<task type="auto">
  <name>Task 5: Capture CW-10 evidence and finalize validation checklist</name>
  <files>.planning/phases/03-end-to-end-validation/03-VALIDATION-CHECKLIST.md</files>
  <read_first>
    - .planning/phases/03-end-to-end-validation/03-VALIDATION-CHECKLIST.md
    - .librespin/state.md
    - .librespin/01-requirements/requirements.yaml
  </read_first>
  <action>
    After the user confirms CW-10 gap-fill results:

    IF CW-10 GAP-FILL PASSED:
    1. Read .librespin/state.md and capture the completeness_score value
    2. Update 03-VALIDATION-CHECKLIST.md CW-10 section:
       - Add checkbox: [x] Ran with stripped YAML: initial score below 70
       - Add checkbox: [x] Gap-fill AskUserQuestion prompts appeared for missing fields
       - Add checkbox: [x] Recalculated score >= 70 after gap-fill
       - Update evidence with actual score values (initial and recalculated)
       - Change note to remove "by design review" — replace with actual live evidence
       - Change status to: **Status: VALIDATED** (happy path + gap-fill path both confirmed live)

    IF CW-10 GAP-FILL FAILED:
    1. Document exact failure in CW-10 section (score was wrong, gap-fill did not trigger, error occurred)
    2. If the scoring bug is fixable per D-05, fix it in SKILL.md and note the fix
    3. Add to Bug Fixes Applied table

    FINALLY:
    4. Verify all CW-01 through CW-10 now have either VALIDATED or documented failure status
    5. Update the Context Pressure Observations section if any new observations from the live runs
  </action>
  <verify>
    <automated>grep "Status:" .planning/phases/03-end-to-end-validation/03-VALIDATION-CHECKLIST.md</automated>
  </verify>
  <acceptance_criteria>
    - CW-10 section in 03-VALIDATION-CHECKLIST.md has either gap-fill checkboxes checked OR documented failure
    - `grep "VALIDATED" .planning/phases/03-end-to-end-validation/03-VALIDATION-CHECKLIST.md` returns lines for both CW-01 and CW-10
    - No CW section still says "by design review" as its only validation method
    - All 10 CW sections have a **Status:** line with either VALIDATED or a documented failure
  </acceptance_criteria>
  <done>All Phase 3 verification gaps closed. CW-01 through CW-10 have live execution evidence. Validation checklist is complete.</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. `grep -c "VALIDATED" .planning/phases/03-end-to-end-validation/03-VALIDATION-CHECKLIST.md` should return 10 (all CW requirements)
2. CW-01 section has all 4 checkboxes checked with live evidence
3. CW-10 section documents both happy path (score 98) AND gap-fill path (score < 70 then >= 70 after questions)
4. No "by design review" or "requires live Claude Code session" qualifiers remain as sole validation evidence
5. .librespin/backup-03-03/ preserves original end-to-end run evidence
</verification>

<success_criteria>
- CW-01 interactive AskUserQuestion path exercised in a live Claude Code session with evidence captured
- CW-10 gap-fill path (stripped YAML -> score < 70 -> AskUserQuestion -> score >= 70) exercised live with evidence captured
- 03-VALIDATION-CHECKLIST.md updated with all checkboxes and evidence for both gaps
- Any blocking bugs found during live testing fixed per D-05 and documented
</success_criteria>

<output>
After completion, create `.planning/phases/03-end-to-end-validation/03-04-SUMMARY.md`
</output>
