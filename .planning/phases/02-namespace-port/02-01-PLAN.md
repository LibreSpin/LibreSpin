---
phase: 02-namespace-port
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/agents/librespin-concept.md
  - .claude/librespin/templates/requirements.yaml
  - .claude/librespin/templates/concept-template.md
  - .claude/librespin/templates/overview-template.md
autonomous: true
requirements: [SKL-02, SKL-03]

must_haves:
  truths:
    - "Worker agent file has correct frontmatter and brief body describing its role"
    - "Requirements YAML template matches hw-concept original with librespin paths"
    - "Concept template matches hw-concept original exactly (no path refs to change)"
    - "Overview template matches hw-concept original exactly (no path refs to change)"
    - "No placeholder TODO stubs remain in any of the four files"
  artifacts:
    - path: ".claude/agents/librespin-concept.md"
      provides: "Flat worker agent reference with frontmatter"
      contains: "name: librespin-concept"
    - path: ".claude/librespin/templates/requirements.yaml"
      provides: "Requirements schema template for users"
      contains: "schema_version: 1"
    - path: ".claude/librespin/templates/concept-template.md"
      provides: "Concept output template"
      contains: "Concept: {CONCEPT_NAME}"
    - path: ".claude/librespin/templates/overview-template.md"
      provides: "Overview comparison template"
      contains: "Architecture Concepts Overview"
  key_links:
    - from: ".claude/agents/librespin-concept.md"
      to: "Claude Code agent registry"
      via: "YAML frontmatter name field"
      pattern: "name: librespin-concept"
    - from: ".claude/librespin/templates/requirements.yaml"
      to: "SKILL.md template loading"
      via: "path reference in SKILL.md"
      pattern: "librespin/templates/requirements.yaml"
---

<objective>
Port the three hw-concept templates and the agent flat file to LibreSpin namespace.

Purpose: Deliver the four small files that Phase 2 requires — the worker agent reference and all three YAML/markdown templates. These files have minimal or no namespace changes (templates are mostly content-neutral; the agent file's frontmatter is already correct from Phase 1).

Output: Four production-ready files replacing Phase 1 placeholders.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-namespace-port/02-CONTEXT.md
@.planning/phases/02-namespace-port/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Port three templates from hw-concept to librespin</name>
  <files>
    .claude/librespin/templates/requirements.yaml
    .claude/librespin/templates/concept-template.md
    .claude/librespin/templates/overview-template.md
  </files>
  <read_first>
    /home/william/repo/hw-concept/.claude/hw-concept/templates/requirements.yaml
    /home/william/repo/hw-concept/.claude/hw-concept/templates/concept-template.md
    /home/william/repo/hw-concept/.claude/hw-concept/templates/overview-template.md
    .claude/librespin/templates/requirements.yaml
    .claude/librespin/templates/concept-template.md
    .claude/librespin/templates/overview-template.md
  </read_first>
  <action>
Replace the three Phase 1 placeholder template files with the full content from hw-concept originals.

**requirements.yaml:** Copy the full 121-line content from `/home/william/repo/hw-concept/.claude/hw-concept/templates/requirements.yaml` verbatim. This file contains no hw-concept path references — it is a pure schema template. Write it to `.claude/librespin/templates/requirements.yaml`. The file header comment should read `# Hardware Concept Requirements Schema` (same as original — this is a content template, not a namespace identifier).

**concept-template.md:** Copy the full 52-line content from `/home/william/repo/hw-concept/.claude/hw-concept/templates/concept-template.md` verbatim. No path references exist in this file. Write to `.claude/librespin/templates/concept-template.md`.

**overview-template.md:** Copy the full 42-line content from `/home/william/repo/hw-concept/.claude/hw-concept/templates/overview-template.md` verbatim. No path references exist in this file. Write to `.claude/librespin/templates/overview-template.md`.

All three files must have zero TODO placeholders remaining. The content is identical to hw-concept originals — these templates describe hardware design structure, not tool namespace.
  </action>
  <verify>
    <automated>grep -r "TODO" .claude/librespin/templates/ && echo "FAIL: TODO found" || echo "PASS: no TODOs"; grep -c "schema_version" .claude/librespin/templates/requirements.yaml; wc -l .claude/librespin/templates/requirements.yaml .claude/librespin/templates/concept-template.md .claude/librespin/templates/overview-template.md</automated>
  </verify>
  <acceptance_criteria>
    - .claude/librespin/templates/requirements.yaml contains "schema_version: 1"
    - .claude/librespin/templates/requirements.yaml contains "project_name:"
    - .claude/librespin/templates/requirements.yaml contains "connectivity:" and "power:" and "sensors:"
    - .claude/librespin/templates/requirements.yaml is 121 lines (matching source)
    - .claude/librespin/templates/concept-template.md contains "Concept: {CONCEPT_NAME}"
    - .claude/librespin/templates/concept-template.md contains "Architectural Characteristics"
    - .claude/librespin/templates/concept-template.md is 52 lines (matching source)
    - .claude/librespin/templates/overview-template.md contains "Architecture Concepts Overview"
    - .claude/librespin/templates/overview-template.md contains "Comparison Matrix"
    - .claude/librespin/templates/overview-template.md is 42 lines (matching source)
    - grep -r "TODO" .claude/librespin/templates/ returns zero matches
    - grep -r "hw-concept" .claude/librespin/templates/ returns zero matches
  </acceptance_criteria>
  <done>All three template files contain full hw-concept content with no TODOs and no hw-concept references.</done>
</task>

<task type="auto">
  <name>Task 2: Port agent flat file with brief body</name>
  <files>.claude/agents/librespin-concept.md</files>
  <read_first>
    /home/william/repo/hw-concept/.claude/agents/hw-concept/AGENT.md
    .claude/agents/librespin-concept.md
  </read_first>
  <action>
Replace the Phase 1 placeholder body in `.claude/agents/librespin-concept.md` while preserving the existing frontmatter (which is already correct per Phase 1).

The existing frontmatter to KEEP EXACTLY AS-IS:
```yaml
---
name: librespin-concept
description: Generate hardware concept designs with BOMs and block diagrams. Supports multi-phase workflow with state persistence.
tools: Read, Write, WebSearch, Grep, Glob, AskUserQuestion
color: blue
---
```

Replace the body (everything after the closing `---`) with a brief purpose statement derived from the hw-concept AGENT.md opening (lines 8-10), adapted to librespin namespace:

```markdown
# LibreSpin Concept Generator Agent

Worker agent spawned by /librespin:concept skill. Executes the hardware concept design workflow phases with fresh context per phase.

Full agent logic and workflow phases are defined in the skill orchestrator at `skills/librespin-concept/SKILL.md`.

## Capabilities

- 9-phase hardware concept design workflow (requirements → drafting → validation → research → generation → critique → refinement → output → comparison)
- Interactive requirements gathering via AskUserQuestion or YAML file import
- Web research for component validation and real part numbers
- State persistence across invocations at `.librespin/state.md`
- Configurable draft count, iteration limit, and confidence threshold
```

Do NOT include the full 6960-line agent body here — per D-01, the full logic lives in SKILL.md, not the agent flat file.
  </action>
  <verify>
    <automated>grep -c "TODO" .claude/agents/librespin-concept.md; grep "name: librespin-concept" .claude/agents/librespin-concept.md; grep "/librespin:concept" .claude/agents/librespin-concept.md; grep "hw-concept" .claude/agents/librespin-concept.md && echo "FAIL: hw-concept found" || echo "PASS: no hw-concept"</automated>
  </verify>
  <acceptance_criteria>
    - .claude/agents/librespin-concept.md line 2 contains "name: librespin-concept"
    - .claude/agents/librespin-concept.md contains "tools: Read, Write, WebSearch, Grep, Glob, AskUserQuestion"
    - .claude/agents/librespin-concept.md contains "color: blue"
    - .claude/agents/librespin-concept.md contains "/librespin:concept"
    - .claude/agents/librespin-concept.md contains ".librespin/state.md"
    - .claude/agents/librespin-concept.md contains "skills/librespin-concept/SKILL.md"
    - grep "TODO" .claude/agents/librespin-concept.md returns zero matches
    - grep "hw-concept" .claude/agents/librespin-concept.md returns zero matches
    - File is between 15 and 30 lines (brief reference, not full agent body)
  </acceptance_criteria>
  <done>Agent flat file has correct frontmatter, brief body referencing SKILL.md, and zero hw-concept or TODO strings.</done>
</task>

</tasks>

<verification>
All four files ported from hw-concept with zero residual hw-concept strings and zero TODO placeholders:
```bash
grep -r "hw-concept" .claude/agents/librespin-concept.md .claude/librespin/templates/ && echo "FAIL" || echo "PASS: zero hw-concept"
grep -r "TODO" .claude/agents/librespin-concept.md .claude/librespin/templates/ && echo "FAIL" || echo "PASS: zero TODOs"
```
</verification>

<success_criteria>
- Four files replaced: agent flat file + three templates
- Zero "hw-concept" strings in any of the four files
- Zero "TODO" strings in any of the four files
- Agent file is a brief reference (~20 lines), not the full 6960-line body
- Template files match hw-concept originals in content and line count
</success_criteria>

<output>
After completion, create `.planning/phases/02-namespace-port/02-01-SUMMARY.md`
</output>
