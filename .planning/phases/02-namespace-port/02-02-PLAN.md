---
phase: 02-namespace-port
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/skills/librespin-concept/SKILL.md
autonomous: true
requirements: [SKL-01, SKL-04, NSP-01, NSP-02, NSP-03, NSP-04, NSP-05, NSP-06]

must_haves:
  truths:
    - "SKILL.md begins with correct frontmatter and /librespin:concept heading"
    - "SKILL.md contains the full orchestrator logic (argument parsing, state load, agent spawn)"
    - "SKILL.md contains the full 9-phase agent body content from hw-concept AGENT.md"
    - "Zero occurrences of hw-concept remain in the file"
    - "All output paths reference .librespin/ not .planning/hw-concept/"
    - "State file path is .librespin/state.md"
    - "Config file path is .librespin/config.yaml"
    - "All Tool references use Agent not Task"
    - "The --output DIR parameter is completely removed"
    - "Config schema references draft_count, iteration_limit, confidence_threshold"
    - "/librespin:concept command heading is present"
  artifacts:
    - path: ".claude/skills/librespin-concept/SKILL.md"
      provides: "Complete skill orchestrator + agent body"
      min_lines: 7000
      contains: "/librespin:concept"
  key_links:
    - from: ".claude/skills/librespin-concept/SKILL.md"
      to: "Claude Code skill registry"
      via: "SKILL.md filename convention + /librespin:concept heading"
      pattern: "# /librespin:concept"
    - from: ".claude/skills/librespin-concept/SKILL.md"
      to: ".librespin/ output directory"
      via: "hardcoded path references"
      pattern: "\\.librespin/"
    - from: ".claude/skills/librespin-concept/SKILL.md"
      to: ".claude/librespin/templates/"
      via: "template loading path"
      pattern: "librespin/templates/requirements.yaml"
    - from: ".claude/skills/librespin-concept/SKILL.md"
      to: "Agent tool"
      via: "allowed-tools frontmatter + await call"
      pattern: "Agent"
---

<objective>
Port the hw-concept command orchestrator and full AGENT.md body into a single SKILL.md file with complete namespace replacement.

Purpose: This is the critical file — the merged ~7100-line SKILL.md that contains both the orchestrator logic (from commands/hw-concept.md) and the full 9-phase agent workflow (from agents/hw-concept/AGENT.md). All hw-concept references must be replaced, all output paths repointed to .librespin/, all Tool references modernized to Agent, and the dead --output DIR parameter removed.

Output: A single production-ready `.claude/skills/librespin-concept/SKILL.md` replacing the Phase 1 placeholder.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-namespace-port/02-CONTEXT.md
@.planning/phases/02-namespace-port/02-RESEARCH.md

<interfaces>
<!-- Phase 1 SKILL.md placeholder frontmatter (MUST be preserved exactly): -->
```yaml
---
description: LibreSpin hardware concept design workflow
argument-hint: "[--input FILE] [--depth quick|medium|thorough]"
allowed-tools:
  - Read
  - Write
  - Agent
  - AskUserQuestion
  - Glob
  - Bash
  - WebSearch
---
```
Note: --output DIR is already absent from argument-hint (Phase 1 decision). WebSearch is present. Agent (not Task) is listed.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build merged SKILL.md with orchestrator preamble and namespace-replaced agent body</name>
  <files>.claude/skills/librespin-concept/SKILL.md</files>
  <read_first>
    .claude/skills/librespin-concept/SKILL.md
    /home/william/repo/hw-concept/.claude/commands/hw-concept.md
    /home/william/repo/hw-concept/.claude/agents/hw-concept/AGENT.md
  </read_first>
  <action>
Build the merged SKILL.md by combining the orchestrator (from hw-concept command file) and the full agent body (from hw-concept AGENT.md), applying all namespace replacements specified below.

**STRUCTURE OF THE OUTPUT FILE:**

```
[Frontmatter — preserve Phase 1 placeholder exactly]
[Heading: # /librespin:concept]
[Orchestrator body — adapted from commands/hw-concept.md lines 15-157]
[Agent body — adapted from agents/hw-concept/AGENT.md lines 8-6960]
```

**SECTION 1: FRONTMATTER (preserve exactly from Phase 1 placeholder)**
```yaml
---
description: LibreSpin hardware concept design workflow
argument-hint: "[--input FILE] [--depth quick|medium|thorough]"
allowed-tools:
  - Read
  - Write
  - Agent
  - AskUserQuestion
  - Glob
  - Bash
  - WebSearch
---
```

**SECTION 2: ORCHESTRATOR (adapted from commands/hw-concept.md lines 12-157)**

Start with the heading:
```markdown
# /librespin:concept
```

Then include the orchestrator body from commands/hw-concept.md (lines 15-157) with these changes applied:

1. Opening description line: Change "Generate hardware concept designs from requirements file or interactively" to "Generate hardware concept designs with BOMs and block diagrams. Orchestrates librespin-concept agent for multi-phase workflow."

2. **Step 1 (Parse Arguments):** Remove `--output DIR` entirely:
   - Remove the bullet: `- \`--output DIR\`: Output directory. Default: \`./concepts/\``
   - Remove `OUTPUT_DIR="./concepts/"` from parsing pseudocode
   - Remove the `if $ARGUMENTS contains "--output":` block (3 lines)

3. **Step 3 (Load or Initialize State):** Replace paths:
   - `.planning/hw-concept-state.md` → `.librespin/state.md`
   - "Resuming hw-concept project" → "Resuming librespin-concept project"
   - "Initializing new hw-concept project" → "Initializing new librespin-concept project"

4. **Step 4 (Spawn Agent):** Replace:
   - "Use Task tool with subagent_type" → "Use Agent tool with subagent_type"
   - `subagent_type="hw-concept"` → `subagent_type="librespin-concept"`
   - "Execute hw-concept workflow" → "Execute librespin-concept workflow"
   - Remove `OUTPUT_DIR: [path]` from Parameters list
   - `.planning/hw-concept-config.yaml` → `.librespin/config.yaml`
   - `.planning/hw-concept-state.md` → `.librespin/state.md`

5. **Step 5 (Handle Agent Return):** Replace:
   - "Run /hw-concept again" → "Run /librespin:concept again"
   - "Run /hw-concept to continue" → "Run /librespin:concept to continue"

6. **Examples section:** Replace:
   - `/hw-concept` → `/librespin:concept` (4 occurrences)
   - Remove the `--output ./my-project/concepts/` example entirely (that example line combines --input, --output, --depth — rewrite as: `/librespin:concept --input reqs.yaml --depth thorough`)

7. **Notes section:** Replace:
   - `.planning/hw-concept-state.md` → `.librespin/state.md`
   - `.planning/hw-concept-config.yaml` → `.librespin/config.yaml`

**SECTION 3: AGENT BODY (adapted from AGENT.md lines 8-6960)**

Append the full content of AGENT.md starting from line 8 ("# Hardware Concept Generator Agent") through line 6960, with these systematic replacements:

**String replacement table (apply globally, in order):**

| Find | Replace | Notes |
|------|---------|-------|
| `.planning/hw-concept-state.md` | `.librespin/state.md` | State file path (2 occurrences) |
| `.planning/hw-concept-config.yaml` | `.librespin/config.yaml` | Config file path (12+ occurrences) |
| `.planning/hw-concept/` | `.librespin/` | Output directory (67 occurrences) |
| `.claude/hw-concept/templates/` | `.claude/librespin/templates/` | Template path (2 occurrences) |
| `/hw-concept` | `/librespin:concept` | Command references (varies) |
| `hw-concept` | `librespin-concept` | Remaining identifier references (agent name, subagent_type, etc.) |
| `await Task({` | `await Agent({` | Tool call on line 3482 |
| `Task tool` | `Agent tool` | Prose references to Tool name |

**Additional specific changes in the agent body:**

1. Line 8 heading: `# Hardware Concept Generator Agent` stays as-is (it describes what the agent does, not a namespace).

2. Line 10: "Worker agent spawned by /hw-concept command" → "Worker agent spawned by /librespin:concept skill"

3. Line 14: "Received from orchestrator command via Task tool prompt" → "Received from orchestrator skill via Agent tool prompt"

4. PARAMETERS section: Remove `OUTPUT_DIR` parameter entirely (the bullet on line 17: `- \`OUTPUT_DIR\`: Directory for generated concepts`). Keep INPUT_FILE, DEPTH, and PHASE parameters.

5. Lines 117-123 (STATE MANAGEMENT): All `.planning/hw-concept-state.md` → `.librespin/state.md`, `.planning/hw-concept-config.yaml` → `.librespin/config.yaml`

6. Line 150 error message: `.claude/hw-concept/templates/requirements.yaml` → `.claude/librespin/templates/requirements.yaml`

7. Line 3482: `await Task({` → `await Agent({`

8. Lines 6906-6909 (INTEGRATION section): `.planning/hw-concept-config.yaml` → `.librespin/config.yaml`, config fields `draft_count`, `iteration_limit`, `confidence_threshold` remain as-is (these are correct per SKL-04).

9. Line 6912: `.planning/hw-concept-state.md` → `.librespin/state.md`

10. Line 6945: `.planning/hw-concept/07-final-output/` → `.librespin/07-final-output/`

11. Line 6952: `.claude/hw-concept/templates/requirements.yaml` → `.claude/librespin/templates/requirements.yaml`

**IMPORTANT CONSTRAINTS:**
- This is a FIDELITY PORT. Do not restructure, rewrite, or "improve" any content. The only changes are namespace replacements and the specific removals listed above.
- Do not split the file (OPT-01 is deferred to v2).
- The final file should be approximately 7050-7100 lines (6960 agent body + ~140 orchestrator - removed lines).
- Every `hw-concept` string in the final file should be zero. Every `.planning/hw-concept` path should be zero.
  </action>
  <verify>
    <automated>grep -c "hw-concept" .claude/skills/librespin-concept/SKILL.md; grep -c "OUTPUT_DIR" .claude/skills/librespin-concept/SKILL.md; grep -c "await Task(" .claude/skills/librespin-concept/SKILL.md; grep -c "/librespin:concept" .claude/skills/librespin-concept/SKILL.md; grep -c "\.librespin/" .claude/skills/librespin-concept/SKILL.md; grep -c "draft_count" .claude/skills/librespin-concept/SKILL.md; wc -l .claude/skills/librespin-concept/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "hw-concept" .claude/skills/librespin-concept/SKILL.md returns 0
    - grep -c "OUTPUT_DIR" .claude/skills/librespin-concept/SKILL.md returns 0
    - grep -c "await Task(" .claude/skills/librespin-concept/SKILL.md returns 0
    - grep "# /librespin:concept" .claude/skills/librespin-concept/SKILL.md returns a match
    - grep "allowed-tools:" .claude/skills/librespin-concept/SKILL.md returns a match
    - grep "Agent" .claude/skills/librespin-concept/SKILL.md returns matches (in allowed-tools and await call)
    - grep "\.librespin/state\.md" .claude/skills/librespin-concept/SKILL.md returns matches
    - grep "\.librespin/config\.yaml" .claude/skills/librespin-concept/SKILL.md returns matches
    - grep "draft_count" .claude/skills/librespin-concept/SKILL.md returns at least 1 match
    - grep "iteration_limit" .claude/skills/librespin-concept/SKILL.md returns at least 1 match
    - grep "confidence_threshold" .claude/skills/librespin-concept/SKILL.md returns at least 1 match
    - grep "librespin/templates/requirements.yaml" .claude/skills/librespin-concept/SKILL.md returns matches
    - grep "\.planning/hw-concept" .claude/skills/librespin-concept/SKILL.md returns 0 matches
    - grep "\.claude/hw-concept" .claude/skills/librespin-concept/SKILL.md returns 0 matches
    - wc -l returns between 7000 and 7150 lines
    - head -1 .claude/skills/librespin-concept/SKILL.md returns "---" (frontmatter start)
    - grep "TODO.*Phase 2" .claude/skills/librespin-concept/SKILL.md returns 0 matches
  </acceptance_criteria>
  <done>SKILL.md contains merged orchestrator + full agent body with all namespace replacements applied, zero hw-concept strings, zero OUTPUT_DIR references, Agent tool (not Task), all .librespin/ paths, and config schema with draft_count/iteration_limit/confidence_threshold.</done>
</task>

<task type="auto">
  <name>Task 2: Semantic verification sweep of SKILL.md</name>
  <files>.claude/skills/librespin-concept/SKILL.md</files>
  <read_first>
    .claude/skills/librespin-concept/SKILL.md
  </read_first>
  <action>
After Task 1 creates SKILL.md, perform a thorough verification sweep to catch edge cases the mechanical replacement might miss.

**Verification checks to run:**

1. **Zero hw-concept check:**
   ```bash
   grep -in "hw-concept" .claude/skills/librespin-concept/SKILL.md
   ```
   Must return zero results. If any found, fix them using the replacement table from Task 1.

2. **Zero Task tool check (case-sensitive):**
   ```bash
   grep -n "Task(" .claude/skills/librespin-concept/SKILL.md
   grep -n "allowed-tools:" -A 10 .claude/skills/librespin-concept/SKILL.md | grep "Task"
   ```
   Must return zero results for both. Note: the word "task" in lowercase prose (e.g., "this task involves") is fine — only the Tool name `Task` with capital T followed by `(` or in `allowed-tools` is a problem.

3. **Zero OUTPUT_DIR check:**
   ```bash
   grep -n "OUTPUT_DIR" .claude/skills/librespin-concept/SKILL.md
   grep -n "\-\-output" .claude/skills/librespin-concept/SKILL.md
   ```
   Must return zero results for both.

4. **Path consistency check:**
   ```bash
   grep -n "\.planning/hw-concept" .claude/skills/librespin-concept/SKILL.md
   grep -n "\.claude/hw-concept" .claude/skills/librespin-concept/SKILL.md
   ```
   Must return zero results for both.

5. **Positive checks (must exist):**
   ```bash
   grep -c "\.librespin/" .claude/skills/librespin-concept/SKILL.md
   grep -c "librespin-concept" .claude/skills/librespin-concept/SKILL.md
   grep -c "/librespin:concept" .claude/skills/librespin-concept/SKILL.md
   ```
   All must return >0.

6. **Frontmatter integrity:**
   ```bash
   head -12 .claude/skills/librespin-concept/SKILL.md
   ```
   Must show the exact frontmatter block: description, argument-hint (no --output), allowed-tools list with Agent (not Task).

7. **Config schema presence (SKL-04):**
   ```bash
   grep -n "draft_count\|iteration_limit\|confidence_threshold" .claude/skills/librespin-concept/SKILL.md
   ```
   All three must appear at least once.

If ANY check fails, fix the specific issue in SKILL.md. Do not rewrite the entire file — make targeted edits only.
  </action>
  <verify>
    <automated>echo "=== hw-concept ===" && grep -c "hw-concept" .claude/skills/librespin-concept/SKILL.md && echo "=== Task( ===" && grep -c "Task(" .claude/skills/librespin-concept/SKILL.md && echo "=== OUTPUT_DIR ===" && grep -c "OUTPUT_DIR" .claude/skills/librespin-concept/SKILL.md && echo "=== old paths ===" && grep -c "\.planning/hw-concept\|\.claude/hw-concept" .claude/skills/librespin-concept/SKILL.md; echo "=== librespin paths ===" && grep -c "\.librespin/" .claude/skills/librespin-concept/SKILL.md; echo "=== config schema ===" && grep -c "draft_count" .claude/skills/librespin-concept/SKILL.md && grep -c "iteration_limit" .claude/skills/librespin-concept/SKILL.md && grep -c "confidence_threshold" .claude/skills/librespin-concept/SKILL.md; echo "=== line count ===" && wc -l .claude/skills/librespin-concept/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "hw-concept" .claude/skills/librespin-concept/SKILL.md outputs "0"
    - grep -c "Task(" .claude/skills/librespin-concept/SKILL.md outputs "0"
    - grep -c "OUTPUT_DIR" .claude/skills/librespin-concept/SKILL.md outputs "0"
    - grep -c "\.planning/hw-concept" .claude/skills/librespin-concept/SKILL.md outputs "0"
    - grep -c "\.claude/hw-concept" .claude/skills/librespin-concept/SKILL.md outputs "0"
    - grep -c "\.librespin/" .claude/skills/librespin-concept/SKILL.md outputs a number greater than 60
    - grep -c "draft_count" outputs at least 1
    - grep -c "iteration_limit" outputs at least 1
    - grep -c "confidence_threshold" outputs at least 1
    - wc -l outputs a number between 7000 and 7150
  </acceptance_criteria>
  <done>SKILL.md passes all verification checks: zero residual hw-concept strings, zero Task tool references, zero OUTPUT_DIR, zero old paths, positive matches for all librespin paths and config schema fields.</done>
</task>

</tasks>

<verification>
Complete namespace port verification for SKILL.md:
```bash
# Zero residual references
grep -r "hw-concept" .claude/skills/librespin-concept/SKILL.md | wc -l  # must be 0
grep -c "Task(" .claude/skills/librespin-concept/SKILL.md              # must be 0
grep -c "OUTPUT_DIR" .claude/skills/librespin-concept/SKILL.md         # must be 0
grep -c "\.planning/hw-concept" .claude/skills/librespin-concept/SKILL.md  # must be 0

# Positive references
grep -c "\.librespin/" .claude/skills/librespin-concept/SKILL.md       # must be >60
grep -c "Agent" .claude/skills/librespin-concept/SKILL.md              # must be >0
grep "# /librespin:concept" .claude/skills/librespin-concept/SKILL.md  # must match

# Config schema (SKL-04)
grep "draft_count" .claude/skills/librespin-concept/SKILL.md           # must match
grep "iteration_limit" .claude/skills/librespin-concept/SKILL.md       # must match
grep "confidence_threshold" .claude/skills/librespin-concept/SKILL.md  # must match
```
</verification>

<success_criteria>
- SKILL.md is a single merged file containing orchestrator preamble + full agent body
- File is 7000-7150 lines (approximately 6960 + ~140 orchestrator - removed lines)
- Zero "hw-concept" strings in the entire file
- Zero "Task(" tool calls — all use "Agent("
- Zero "OUTPUT_DIR" references
- Zero ".planning/hw-concept" paths
- All output paths use ".librespin/"
- State file referenced as ".librespin/state.md"
- Config file referenced as ".librespin/config.yaml"
- Config schema includes draft_count, iteration_limit, confidence_threshold
- /librespin:concept heading present
- Correct frontmatter preserved from Phase 1 placeholder
</success_criteria>

<output>
After completion, create `.planning/phases/02-namespace-port/02-02-SUMMARY.md`
</output>
